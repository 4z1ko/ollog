# ollog
This project is ollog, a self-hosted ham-radio logbook. Its main job is to let multiple radio operators log QSOs, meaning radio contacts, while keeping each operator’s log isolated. It stores contact data in ADIF-style fields, supports import/export, has a browser UI, exposes a REST API, listens for UDP ADIF records from logging software
