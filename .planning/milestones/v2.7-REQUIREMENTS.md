# Requirements Archive: v2.7 UTC Date/Time Entry

**Archived:** 2026-05-02
**Status:** ✅ All requirements satisfied and shipped

## Overview

Upgrade the Log QSO form with live UTC date/time defaults, lock/unlock toggles,
HHMMSS precision, and post-submission reset behavior control.

## v1 Requirements — All Complete

### Date Field (DATE)

- [x] **DATE-01**: Date field defaults to today's UTC date in `YYYYMMDD` format on form load
- [x] **DATE-02**: Date field is locked (`readonly`) by default — value is included in form submission
- [x] **DATE-03**: A lock icon button (16×16, closed padlock when locked / open when unlocked)
      next to the date field toggles between readonly and editable
- [x] **DATE-04**: Manual date input is validated against `YYYYMMDD` format; invalid input is
      rejected with visible feedback before submission

### Time Field (TIME)

- [x] **TIME-01**: Time field defaults to current UTC time in `HHMMSS` format on form load
- [x] **TIME-02**: While locked, the time field auto-updates every second via `setInterval`
      using `Date.getUTC*()` methods (no local-timezone leakage)
- [x] **TIME-03**: A lock icon button (16×16) next to the time field toggles auto-update off
      and makes the field editable
- [x] **TIME-04**: Manual time input accepts either `HHMM` (4 digits) or `HHMMSS` (6 digits);
      `HHMM` is normalized to `HHMM00` before submission
- [x] **TIME-05**: Manual time input is validated; invalid formats are rejected with visible
      feedback before submission

### Database (DB)

- [x] **DB-01**: Existing `TIME_ON` values stored as `HHMM` (4 digits) are migrated to
      `HHMM00` (6 digits) at app startup; migration is idempotent (safe to re-run)
- [x] **DB-02**: Server-side `TIME_ON` validation accepts both `HHMM` (4 digits) and
      `HHMMSS` (6 digits)

### Post-Submit Behavior (RESET)

- [x] **RESET-01**: A toggle on the Log QSO form controls post-submission behavior:
      "Keep current date/time" vs "Reset to live UTC"
- [x] **RESET-02**: "Keep current date/time" — after QSO submission, field values and
      lock/unlock state are preserved exactly as they were before submission
- [x] **RESET-03**: "Reset to live UTC" — after QSO submission, both fields return to
      locked state with date set to today's UTC and time auto-updating

## Future Requirements

- Keyboard shortcut to toggle date/time lock
- Date picker calendar widget for manual date selection
- Per-operator reset preference persisted server-side (currently localStorage)

## Out of Scope

- `<input type="datetime-local">` — incompatible with ADIF's separate QSO_DATE/TIME_ON field structure
- Changing the ADIF TIME_ON field to a typed datetime model — ADIF stores strings verbatim
- Band/mode/freq preservation in "Keep current" mode — only date/time fields specified

## Traceability

| REQ-ID   | Phase    | Status   | Outcome |
|----------|----------|----------|---------|
| DB-01    | Phase 52 | Complete | `normalize_time_on()` idempotent migration via anchored regex + aggregation pipeline |
| DB-02    | Phase 52 | Complete | `parse_adif_datetime()` confirmed to accept HHMM and HHMMSS (no code change, 5 tests) |
| DATE-01  | Phase 53 | Complete | `initDateTime()` populates date from `getUTCFullYear/Month/Date` on page load |
| DATE-02  | Phase 53 | Complete | `readonly` attribute on QSO_DATE input; `initDateTime()` enforces `readOnly=true` |
| DATE-03  | Phase 53 | Complete | Padlock click handler toggles readOnly, SVG path, aria-label, locked styling |
| DATE-04  | Phase 53 | Complete | `validate()` checks `QSO_DATE` against `/^\d{8}$/`; rejects with `form-input-error` ring |
| TIME-01  | Phase 53 | Complete | `initDateTime()` populates time from `getUTCHours/Minutes/Seconds` on page load |
| TIME-02  | Phase 53 | Complete | `setInterval` in `initDateTime()` ticks `currentUTCTime()` every 1000ms while locked |
| TIME-03  | Phase 53 | Complete | Padlock click handler: `clearInterval` on unlock, new `setInterval` on re-lock |
| TIME-04  | Phase 53 | Complete | `htmx:beforeRequest` normalizer: `/^\d{4}$/` test → append `"00"` before validate |
| TIME-05  | Phase 53 | Complete | Range-checking regex `/^([01]\d\|2[0-3])([0-5]\d)([0-5]\d)$/` rejects impossible times |
| RESET-01 | Phase 53 | Complete | `initResetToggle()` IIFE with `localStorage.setItem/getItem('ollog.resetMode')` |
| RESET-02 | Phase 53 | Complete | `htmx:afterSwap` skips `form.reset()` in `keep` mode; field state preserved |
| RESET-03 | Phase 53 | Complete | `htmx:afterSwap` calls `form.reset()` + `initDateTime()` via `setTimeout(0)` deferral |
