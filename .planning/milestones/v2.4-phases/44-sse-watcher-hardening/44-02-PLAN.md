---
phase: 44-sse-watcher-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/log/log.html
autonomous: false
requirements:
  - LIVE-02

must_haves:
  truths:
    - "The LIVE indicator does NOT turn green on bare SSE connection open (htmx:sseOpen)"
    - "The LIVE indicator turns green only after the first htmx:sseMessage with type='new_qso' is received"
    - "The LIVE indicator shows OFFLINE on htmx:sseError"
    - "The LIVE indicator is hidden on htmx:sseClose"
    - "The eventsFlowing boolean is reset to false on both sseError and sseClose"
  artifacts:
    - path: "templates/log/log.html"
      provides: "LIVE indicator state machine driven by SSE message events, not connection open"
      contains: "eventsFlowing"
  key_links:
    - from: "templates/log/log.html"
      to: "/feed/station"
      via: "htmx:sseMessage event listener"
      pattern: "htmx:sseMessage"
---

<objective>
Fix the LIVE indicator so it accurately reflects whether SSE events are flowing, not merely whether the HTTP connection is open.

Purpose: The current indicator turns green on `htmx:sseOpen`, which masks a dead watcher (the HTTP connection stays alive even when the watcher task has died). After this fix, the indicator turns green only after the first `new_qso` message is received, proving the watcher is alive and sending data.

Output: Updated `templates/log/log.html` with the corrected LIVE indicator IIFE.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-sse-watcher-hardening/44-RESEARCH.md
@.planning/phases/44-sse-watcher-hardening/44-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From templates/log/log.html (current IIFE, lines 114-152):
```javascript
(function () {
    var indicator = document.getElementById('live-indicator');
    // htmx:sseOpen -- currently sets green (BUG)
    // htmx:sseError -- shows OFFLINE text
    // htmx:sseClose -- hides indicator
    // htmx:sseMessage -- triggers htmx.ajax refresh on new_qso
})();
```

From templates/log/log.html (indicator HTML, line 18-23):
```html
<span id="live-indicator"
      class="hidden items-center gap-1.5 px-2.5 py-1 rounded-full text-xs font-semibold
             bg-emerald-100 dark:bg-emerald-900/40 text-emerald-700 dark:text-emerald-400">
  <span class="w-1.5 h-1.5 rounded-full bg-emerald-500 animate-pulse inline-block"></span>
  LIVE
</span>
```

Note: The dark-mode Tailwind classes `dark:bg-emerald-900/40` and `dark:text-emerald-400` already exist in the HTML (line 20). No new dark: classes are introduced. `npm run verify` should still pass without changes.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace LIVE indicator IIFE with message-first state machine</name>
  <files>templates/log/log.html</files>
  <read_first>
    - templates/log/log.html (current state -- 153 lines, the entire template)
    - .planning/phases/44-sse-watcher-hardening/44-RESEARCH.md (Pattern 3: LIVE Indicator Message-First State Machine)
    - .planning/phases/44-sse-watcher-hardening/44-PATTERNS.md (log.html fixed state machine pattern)
  </read_first>
  <action>
Replace the entire `<script>` block in `templates/log/log.html` (lines 114-152) with the following corrected IIFE. The HTML above line 114 and the `{% endblock %}` on line 153 are NOT changed.

Replace:
```html
<script>
  (function () {
    var indicator = document.getElementById('live-indicator');

    document.body.addEventListener('htmx:sseOpen', function (e) {
      if (e.detail.elt && e.detail.elt.id === 'log-table') {
        indicator.classList.remove('hidden');
        indicator.classList.add('flex');
        indicator.querySelector('span:last-child').textContent = 'LIVE';
        indicator.className = indicator.className
          .replace('bg-rose-100 dark:bg-rose-900/40 text-rose-700 dark:text-rose-400', '')
          + ' bg-emerald-100 dark:bg-emerald-900/40 text-emerald-700 dark:text-emerald-400';
      }
    });

    document.body.addEventListener('htmx:sseError', function (e) {
      if (e.detail.elt && e.detail.elt.id === 'log-table') {
        indicator.classList.remove('hidden');
        indicator.classList.add('flex');
        indicator.querySelector('span:last-child').textContent = 'OFFLINE';
      }
    });

    document.body.addEventListener('htmx:sseClose', function (e) {
      if (e.detail.elt && e.detail.elt.id === 'log-table') {
        indicator.classList.add('hidden');
        indicator.classList.remove('flex');
      }
    });

    document.body.addEventListener('htmx:sseMessage', function (e) {
      if (!e.detail || !e.detail.elt || e.detail.elt.id !== 'log-table') return;
      if (e.detail.type !== 'new_qso') return;
      if (!document.getElementById('auto-refresh-ok')) return;
      if (document.querySelector('#log-table input')) return;
      htmx.ajax('GET', '/log/view', { target: '#log-table', swap: 'innerHTML' });
    });
  })();
</script>
```

With this exact replacement:
```html
<script>
  (function () {
    var indicator = document.getElementById('live-indicator');
    var eventsFlowing = false;

    document.body.addEventListener('htmx:sseOpen', function (e) {
      if (e.detail.elt && e.detail.elt.id === 'log-table') {
        // Connection opened -- do NOT set green. Wait for first actual event.
        // A dead watcher keeps the HTTP connection alive but sends no events.
      }
    });

    document.body.addEventListener('htmx:sseMessage', function (e) {
      if (!e.detail || !e.detail.elt || e.detail.elt.id !== 'log-table') return;
      if (e.detail.type !== 'new_qso') return;
      if (!eventsFlowing) {
        eventsFlowing = true;
        indicator.classList.remove('hidden');
        indicator.classList.add('flex');
        indicator.querySelector('span:last-child').textContent = 'LIVE';
        indicator.classList.remove('bg-rose-100', 'text-rose-700');
        indicator.classList.add('bg-emerald-100', 'dark:bg-emerald-900/40',
                                'text-emerald-700', 'dark:text-emerald-400');
      }
      if (!document.getElementById('auto-refresh-ok')) return;
      if (document.querySelector('#log-table input')) return;
      htmx.ajax('GET', '/log/view', { target: '#log-table', swap: 'innerHTML' });
    });

    document.body.addEventListener('htmx:sseError', function (e) {
      if (e.detail.elt && e.detail.elt.id === 'log-table') {
        eventsFlowing = false;
        indicator.classList.remove('hidden');
        indicator.classList.add('flex');
        indicator.querySelector('span:last-child').textContent = 'OFFLINE';
      }
    });

    document.body.addEventListener('htmx:sseClose', function (e) {
      if (e.detail.elt && e.detail.elt.id === 'log-table') {
        eventsFlowing = false;
        indicator.classList.add('hidden');
        indicator.classList.remove('flex');
      }
    });
  })();
</script>
```

Key behavioral changes:
1. **`htmx:sseOpen` handler** becomes a no-op comment -- connection open no longer sets green
2. **`htmx:sseMessage` handler** gains the `eventsFlowing` sentinel -- sets LIVE only on first `new_qso` message, proving watcher is alive
3. **`htmx:sseMessage` handler** now comes BEFORE `htmx:sseError` in source order (moved up from last position)
4. **`htmx:sseError` handler** resets `eventsFlowing = false` and shows OFFLINE
5. **`htmx:sseClose` handler** resets `eventsFlowing = false` and hides indicator
6. **classList.remove()/classList.add()** replaces the fragile string `.replace()` + concatenation pattern (RESEARCH.md Pitfall 4)

No new Tailwind dark: classes are introduced. The `dark:bg-emerald-900/40` and `dark:text-emerald-400` classes already exist in the indicator HTML (line 20) and in the existing JS. `npm run verify` is not affected.
  </action>
  <verify>
    <automated>grep -c "eventsFlowing" templates/log/log.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep "var eventsFlowing = false" templates/log/log.html` matches exactly 1 line
    - `grep "eventsFlowing = true" templates/log/log.html` matches exactly 1 line (inside sseMessage handler)
    - `grep "eventsFlowing = false" templates/log/log.html` matches exactly 3 lines (declaration + sseError + sseClose)
    - `grep "// Connection opened" templates/log/log.html` matches (the no-op comment in sseOpen)
    - `grep "indicator.className = indicator.className" templates/log/log.html` does NOT match (old string replacement pattern removed)
    - The `htmx:sseOpen` handler body contains only comments (no classList or textContent manipulation)
    - `grep "setLive()" templates/log/log.html` does NOT match (no function call -- the logic is inline in the sseMessage handler)
    - The `htmx:sseMessage` handler checks `e.detail.type !== 'new_qso'` before setting the indicator green
  </acceptance_criteria>
  <done>LIVE indicator IIFE replaced with message-first state machine; green state triggered only on first new_qso message, not on connection open; OFFLINE shown on sseError; hidden on sseClose; eventsFlowing sentinel manages state transitions</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify LIVE indicator behavior in browser</name>
  <files>templates/log/log.html</files>
  <action>
Human verification of the LIVE indicator state machine in a browser. No code changes -- this task validates the work done in Task 1.

What was built: The LIVE indicator state machine was rewritten so it turns green only after a real new_qso SSE message is received (not on bare connection open), shows OFFLINE on SSE error, and hides on SSE close.

Steps to verify:
1. Start the app: `docker-compose up -d` (or `uvicorn app.main:app --reload`)
2. Open `/log/view` in Chrome with DevTools open (Network tab)
3. Observe the LIVE indicator is NOT visible (hidden) even though the SSE connection to `/feed/station` is open in the Network tab
4. Insert a QSO (via the form, REST API POST, or UDP)
5. Observe the LIVE indicator turns green with "LIVE" text after the SSE event frame appears in the EventStream tab
6. Stop the server (Ctrl+C or docker-compose down)
7. Observe the indicator shows "OFFLINE" text on SSE error, then hides on SSE close
  </action>
  <verify>
    <automated>grep -c "eventsFlowing" templates/log/log.html</automated>
  </verify>
  <done>Human confirms: indicator hidden before first event, green after first new_qso message, OFFLINE on error, hidden on close</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. The LIVE indicator is a purely cosmetic client-side DOM element. It does not make authentication decisions, transmit data, or expose server internals.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| -- | -- | -- | -- | No security-relevant changes in this plan. The indicator reads from existing SSE events already delivered to the authenticated client. No auth, input validation, or access control modifications. |

Phase 44 makes no changes to authentication, session management, access control, input validation, or cryptography. RESEARCH.md confirms: "No security concerns for this phase."
</threat_model>

<verification>
1. `grep "eventsFlowing" templates/log/log.html` -- confirms sentinel variable exists
2. `grep "// Connection opened" templates/log/log.html` -- confirms sseOpen is a no-op
3. Manual browser test: indicator hidden on page load, green on first new_qso event, OFFLINE on error
4. `npm run verify` -- Tailwind dark mode classes still present (no new classes added)
</verification>

<success_criteria>
- LIVE indicator does not turn green on SSE connection open
- LIVE indicator turns green only after first new_qso SSE message
- LIVE indicator shows OFFLINE on SSE error
- LIVE indicator hides on SSE close
- No new Tailwind classes introduced
- Human verification confirms correct browser behavior
</success_criteria>

<output>
After completion, create `.planning/phases/44-sse-watcher-hardening/44-02-SUMMARY.md`
</output>
