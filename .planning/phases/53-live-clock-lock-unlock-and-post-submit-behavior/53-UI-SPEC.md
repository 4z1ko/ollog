---
phase: 53
slug: live-clock-lock-unlock-and-post-submit-behavior
status: approved
reviewed_at: 2026-04-28T00:00:00Z
shadcn_initialized: false
preset: none
created: 2026-04-28
---

# Phase 53 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — manual Tailwind CSS component library |
| Preset | not applicable |
| Component library | none (custom `@layer components` in `static/css/input.css`) |
| Icon library | Heroicons 2.x outline — inline SVG only, no npm package |
| Font | `-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif` (system stack) |

**Source:** `tailwind.config.js`, `static/css/input.css` — VERIFIED codebase

---

## Spacing Scale

Declared values (multiples of 4, matching existing Tailwind 8-point usage in templates):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps (`gap-1`, `p-1`), inline icon padding |
| sm | 8px | Compact element spacing (`gap-2`, `px-2`) |
| md | 16px | Default element spacing (`gap-4`, `px-4`) |
| lg | 24px | Card body padding (`px-6 py-5` = 24/20px) |
| xl | 32px | Layout gaps between major form sections |
| 2xl | 48px | Major section breaks (page-level) |
| 3xl | 64px | Page-level spacing |

Exceptions:
- Padlock button target area: `inset-y-0 right-0 pr-3` (12px) — right-side icon tap zone within input wrapper. Source: CONTEXT.md D-04. Updated from pr-2.5 (10px, not on 8-point grid) to pr-3 (12px, on 4-point grid).
- Input right padding to clear padlock icon: `pr-9` (36px) on `QSO_DATE` and `TIME_ON` inputs. Source: RESEARCH.md Pattern 2.
- Toggle switch pill: `w-9 h-5` (36px × 20px), thumb `h-4 w-4` (16px) — exact pill-and-thumb proportions for the `peer-checked:` toggle. Source: RESEARCH.md Pattern 4.

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body / form input value | 14px (`text-sm`) | 400 (regular) | 1.5 |
| Form label | 12px (`text-xs`) | 600 (semibold), uppercase, tracked | 1.2 |
| Reset mode label | 12px (`text-xs`) | 400 (regular) | 1.5 |
| Page heading | 20px (`text-xl`) | 600 (semibold) | 1.2 |

Two weights only: 400 (regular) and 600 (semibold). Weight 700 (bold) is not used in this phase; the page heading uses `font-semibold` (`font-bold` dropped to satisfy the 2-weight maximum).

Font is monospace (`font-mono`) for input values in `QSO_DATE` and `TIME_ON` fields — ensures fixed-width digit columns for the live clock display.

**Source:** `static/css/input.css` `.form-label` (`text-xs font-semibold uppercase tracking-wider`), `.form-input` (`text-sm`), `templates/log/form.html` heading updated to `text-xl font-semibold` — VERIFIED codebase

---

## Visual Hierarchy

**Primary visual anchor:** The `QSO_DATE` and `TIME_ON` fields with their inline padlock icons. These are the new interactive elements in this phase. The live clock ticking in `TIME_ON` draws the eye and communicates real-time UTC state.

**Secondary element:** The reset-mode toggle switch in the submit row. It is visually subordinate to the primary CTA ("Log QSO") and communicates a behavioral preference rather than a core action.

**Tertiary:** All other form fields are unchanged and carry no new visual weight.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#f2f2f7` light / `#0f0f0f` dark | Page canvas (`canvas-light` / `canvas-dark`) |
| Secondary (30%) | `#ffffff` light / `#1c1c1e` dark | Card surface (`surface-light` / `surface-dark`) |
| Accent (10%) | `#4f46e5` (`indigo-600`) | Submit button, focus rings, toggle pill when active (`peer-checked:bg-indigo-500`) |
| Destructive | `#e11d48` (`rose-600`) | Validation error ring (`.form-input-error`), error alert background |

**Accent reserved for:** submit button (`btn-primary`), input focus ring (`focus:ring-indigo-500`), reset-mode toggle pill when checked (`peer-checked:bg-indigo-500`). Must NOT be used on padlock icons or the Clear button.

**Locked field visual state:**
- Light mode: `bg-gray-50` — distinct from white input background
- Dark mode: `bg-gray-800/50` — translucent over dark surface
- Cursor: `cursor-not-allowed` on both light and dark
- Source: CONTEXT.md D-06, RESEARCH.md Existing Tailwind Color References — VERIFIED codebase

**Padlock icon color:**
- Default: `text-gray-400`
- Hover: `text-gray-600` light / `text-gray-300` dark

**Toggle switch off state:**
- Pill: `bg-gray-300` light / `bg-gray-600` dark
- Thumb: `after:bg-white`

**Source:** `tailwind.config.js` color tokens, `static/css/input.css` component classes — VERIFIED codebase

---

## Component Inventory

All components already exist in `static/css/input.css`. No new CSS component classes are added to `input.css` in this phase.

| Component | Class | Phase 53 Use |
|-----------|-------|--------------|
| Form input | `.form-input` | QSO_DATE and TIME_ON base style |
| Form label | `.form-label` | "Date (UTC)", "Time (UTC)" labels |
| Primary button | `.btn-primary` | "Log QSO" submit button (unchanged) |
| Ghost button | `.btn-ghost .btn-sm` | "Clear" reset button (unchanged) |
| Error ring | `.form-input-error` | Inline validation error on date/time fields |
| Card | `.card` | Form card wrapper (unchanged) |
| Card header | `.card-header` | "New Contact" header (unchanged) |
| Card body | `.card-body` | Form content area (unchanged) |

**New inline Tailwind constructs** (not new CSS classes — all inline utilities in `form.html`):

| Construct | Classes | Purpose |
|-----------|---------|---------|
| Input wrapper (relative) | `relative` | Positions the absolute padlock button inside the input |
| Padlock button | `absolute inset-y-0 right-0 flex items-center pr-3 text-gray-400 hover:text-gray-600 dark:hover:text-gray-300` | Lock toggle button positioned at input right |
| Locked input state | `bg-gray-50 dark:bg-gray-800/50 cursor-not-allowed` | Visual locked state applied via JS `applyLockedStyle()` |
| Toggle pill | `relative inline-flex items-center cursor-pointer w-9 h-5 bg-gray-300 dark:bg-gray-600 rounded-full peer-checked:bg-indigo-500 after:content-[''] after:absolute after:top-0.5 after:left-0.5 after:bg-white after:rounded-full after:h-4 after:w-4 after:transition-all peer-checked:after:translate-x-4` | Reset mode toggle switch label |
| Toggle checkbox | `sr-only peer` | Hidden accessible checkbox driving `peer-checked:` variants |
| Toggle label text | `text-xs text-gray-500 dark:text-gray-400` | Dynamic "Reset to live UTC" / "Keep current date/time" text |

**Source:** CONTEXT.md D-01 through D-06, RESEARCH.md Patterns 2 and 4 — VERIFIED decisions

---

## Interaction Contracts

### DATE Field (QSO_DATE)

| State | Visual | Behaviour |
|-------|--------|-----------|
| Locked (default) | `form-input font-mono pr-9` + `bg-gray-50 dark:bg-gray-800/50 cursor-not-allowed` + closed-padlock icon | `readOnly=true`. Value = today UTC in `YYYYMMDD`. Does not update (date is static). |
| Unlocked | `form-input font-mono pr-9` (normal — no locked classes) + open-padlock icon | `readOnly=false`. Free text entry. Value preserved until user changes it. |
| Error | + `.form-input-error` ring | Shows when `validate()` rejects non-`^\d{8}$` value. Only fires if field is unlocked. |

Click padlock → if locked: set `readOnly=false`, swap to open-padlock SVG, remove locked classes. If unlocked: set `readOnly=true`, restore current UTC date, swap to closed-padlock SVG, apply locked classes.

### TIME_ON Field

| State | Visual | Behaviour |
|-------|--------|-----------|
| Locked (default) | `form-input font-mono pr-9` + `bg-gray-50 dark:bg-gray-800/50 cursor-not-allowed` + closed-padlock icon | `readOnly=true`. `setInterval` updates value every 1000ms via `Date.getUTC*()` exclusively. |
| Unlocked | `form-input font-mono pr-9` (normal) + open-padlock icon | `readOnly=false`. `setInterval` cleared. Free text entry. Accepts HHMM (4 digits) or HHMMSS (6 digits). |
| Error | + `.form-input-error` ring | Shows when `validate()` rejects non-`^\d{6}$` value (after normalization). Only fires if field is unlocked. |

Click padlock → if locked: `clearInterval(timeInterval)`, set `readOnly=false`, swap to open-padlock SVG, remove locked classes. If unlocked: set `readOnly=true`, apply locked classes, swap to closed-padlock SVG, restart `setInterval`.

### Padlock Icons

Both padlock buttons: `<button type="button">` — never `type="submit"` or `type="reset"`. Size: `w-4 h-4` (16×16 rendered). Heroicons 2.x outline, `fill="none" stroke-width="1.5" stroke="currentColor" viewBox="0 0 24 24"`.

**Accessibility — aria-label (required on all padlock buttons):**

| Field | Lock state | aria-label value |
|-------|------------|-----------------|
| QSO_DATE | Locked (closed padlock showing) | `"Lock date field"` |
| QSO_DATE | Unlocked (open padlock showing) | `"Unlock date field"` |
| TIME_ON | Locked (closed padlock showing) | `"Lock time field"` |
| TIME_ON | Unlocked (open padlock showing) | `"Unlock time field"` |

The JS toggle handlers must update `aria-label` when swapping the SVG icon. Initial value is the locked-state label (`"Lock date field"` / `"Lock time field"`) since both fields are locked on page load.

Lock-closed path (locked state):
```
M16.5 10.5V6.75C16.5 4.26472 14.4853 2.25 12 2.25C9.51472 2.25 7.5 4.26472 7.5 6.75V10.5M6.75 21.75H17.25C18.4926 21.75 19.5 20.7426 19.5 19.5V12.75C19.5 11.5074 18.4926 10.5 17.25 10.5H6.75C5.50736 10.5 4.5 11.5074 4.5 12.75V19.5C4.5 20.7426 5.50736 21.75 6.75 21.75Z
```

Lock-open path (unlocked state):
```
M13.5 10.5V6.75C13.5 4.26472 15.5147 2.25 18 2.25C20.4853 2.25 22.5 4.26472 22.5 6.75V10.5M3.75 21.75H14.25C15.4926 21.75 16.5 20.7426 16.5 19.5V12.75C16.5 11.5074 15.4926 10.5 14.25 10.5H3.75C2.50736 10.5 1.5 11.5074 1.5 12.75V19.5C1.5 20.7426 2.50736 21.75 3.75 21.75Z
```

Source: https://github.com/tailwindlabs/heroicons — outline 24px. VERIFIED via WebFetch in RESEARCH.md.

### Reset Mode Toggle

Placement: inline with the submit row, same `flex items-center gap-3` flex row as "Log QSO" and "Clear". Source: CONTEXT.md D-02.

Default state: ON ("Reset to live UTC") when `localStorage.getItem('ollog.resetMode')` is null or `'reset'`. Source: CONTEXT.md D-03.

Toggle state labels:
- Checked (ON): `"Reset to live UTC"` — after submit, form resets to locked UTC defaults
- Unchecked (OFF): `"Keep current date/time"` — after submit, fields preserve their values

Persistence: `localStorage` key `ollog.resetMode`, values `'reset'` or `'keep'`.

### Post-Submit Behavior

Both modes always: `clearErrors()`, `submitAttempted = false`, focus `[name="CALL"]`.

"Reset to live UTC" mode additionally: `form.reset()` then immediately `initDateTime()`.
"Keep current date/time" mode: no `form.reset()`, no `initDateTime()`.

Source: CONTEXT.md D-07 through D-09 — VERIFIED decisions.

### Clear Button

The `<button type="reset">` fires the native `reset` event. A `form.addEventListener('reset', ...)` handler must call `initDateTime()` via `setTimeout(fn, 0)` to re-populate after the browser clears fields. Source: CONTEXT.md code_context, RESEARCH.md Pattern 6.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | "Log QSO" (unchanged) |
| Date field label | "Date (UTC)" (unchanged) |
| Time field label | "Time (UTC)" (unchanged) |
| Reset toggle — locked ON | "Reset to live UTC" |
| Reset toggle — locked OFF | "Keep current date/time" |
| Date placeholder (unlocked) | "YYYYMMDD" |
| Time placeholder (unlocked) | "HHMMSS" |
| Empty state heading | n/a — no empty states in this phase |
| Error state — date | No explicit tooltip text. Visual indicator only: `.form-input-error` ring on `QSO_DATE` field. |
| Error state — time | No explicit tooltip text. Visual indicator only: `.form-input-error` ring on `TIME_ON` field. |
| Destructive confirmation | None — no destructive actions in this phase. |

Note: The existing form uses visual-only error rings (no tooltip or helper text beneath the field). This phase maintains that established pattern — no new error message strings are introduced.

---

## Tailwind Purge Contract

The following complete literal class strings MUST appear in `templates/log/form.html` for Tailwind JIT to emit them to `static/css/output.css`. Run `npm run build && npm run verify` after implementing.

| Class | New in Phase 53 | Purpose |
|-------|-----------------|---------|
| `bg-gray-50` | No (exists in input.css) | Locked input light mode bg |
| `dark:bg-gray-800/50` | Yes — must appear in form.html | Locked input dark mode bg |
| `cursor-not-allowed` | No (exists in btn disabled) | Locked input cursor |
| `sr-only` | Likely new in form.html | Toggle checkbox hidden |
| `peer` | Yes | Toggle checkbox peer for `peer-checked:` |
| `peer-checked:bg-indigo-500` | Yes | Toggle pill active color |
| `peer-checked:after:translate-x-4` | Yes | Toggle thumb slide |
| `after:content-['']` | Yes | Toggle thumb pseudo-element |
| `after:absolute` | Yes | Toggle thumb positioning |
| `after:top-0.5` | Yes | Toggle thumb vertical position |
| `after:left-0.5` | Yes | Toggle thumb initial position |
| `after:bg-white` | Yes | Toggle thumb color |
| `after:rounded-full` | Yes | Toggle thumb shape |
| `after:h-4` | Yes | Toggle thumb height |
| `after:w-4` | Yes | Toggle thumb width |
| `after:transition-all` | Yes | Toggle thumb animation |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — shadcn not initialized |
| Third-party | none | not applicable |

No component registry is used in this phase. All UI is constructed from:
- Existing `.form-input`, `.btn-primary`, `.btn-ghost`, `.card`, `.form-input-error` from `static/css/input.css`
- New inline Tailwind utility classes directly in `templates/log/form.html`
- Heroicons inline SVG paths (no npm package — SVG markup inlined directly)

---

## Build Verification Checklist

After implementing `templates/log/form.html`:

1. `npm run build` — recompile `static/css/output.css`
2. `npm run verify` — assert dark mode + color-scheme classes present
3. `grep 'bg-gray-800/50' static/css/output.css` — confirm locked dark bg emitted
4. `grep 'peer-checked' static/css/output.css` — confirm toggle switch classes emitted
5. `uv run pytest tests/ -x -q` — backend regression guard (all tests must pass)
6. Manual browser walkthrough of all 7 success criteria from ROADMAP.md Phase 53

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
