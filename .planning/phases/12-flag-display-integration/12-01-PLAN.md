---
phase: 12-flag-display-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - static/flags/  # 271 SVGs moved from app/static/flags/
  - app/qso/ui_router.py
  - templates/log/qso_row.html
autonomous: true

must_haves:
  truths:
    - "Each QSO row in the log table displays a flag icon next to the callsign when the prefix resolves (e.g. US flag beside W1AW)"
    - "QSO rows where the prefix does not resolve show no flag and no broken image"
    - "Flag icons survive HTMX pagination (Next/Previous) without disappearing or breaking"
    - "Hovering a flag shows the country name as a tooltip"
  artifacts:
    - path: "static/flags/"
      provides: "271 SVG flag files served at /static/flags/*.svg"
    - path: "app/qso/ui_router.py"
      provides: "flag_iso and flag_country keys in _qso_to_view_dict()"
      contains: "lookup_prefix"
    - path: "templates/log/qso_row.html"
      provides: "Conditional flag img tag before callsign text"
      contains: "flag_iso"
  key_links:
    - from: "app/qso/ui_router.py"
      to: "app/callsign/prefixes.py"
      via: "lookup_prefix() call in _qso_to_view_dict()"
      pattern: "lookup_prefix\\(qso\\.CALL\\)"
    - from: "app/qso/ui_router.py"
      to: "pycountry"
      via: "pycountry.countries.get(alpha_2=iso)"
      pattern: "pycountry\\.countries\\.get"
    - from: "templates/log/qso_row.html"
      to: "static/flags/"
      via: "img src path using flag_iso"
      pattern: "/static/flags/.*\\.svg"
---

<objective>
Wire country flag display into the QSO log table. Move flag SVGs to the served static directory, enrich the view-dict with flag metadata from lookup_prefix(), and render conditional flag img tags in the QSO row template.

Purpose: Complete v1.2 milestone — country flags appear next to callsigns in the log view.
Output: Working flag display in log table with tooltip and graceful fallback for unresolvable callsigns.
</objective>

<execution_context>
@/Users/royco/.claude/get-shit-done/workflows/execute-plan.md
@/Users/royco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-flag-display-integration/12-RESEARCH.md
@app/qso/ui_router.py
@templates/log/qso_row.html
@app/callsign/prefixes.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move flag SVGs and add flag enrichment to view-dict</name>
  <files>
    static/flags/ (271 files moved from app/static/flags/)
    app/qso/ui_router.py
  </files>
  <action>
    Step 1 — Relocate flag assets:
    Run `git mv app/static/flags static/flags` to move 271 SVG files from the app-internal
    static directory to the root static directory where the StaticFiles mount serves from.
    After the move, `app/static/flags/` should no longer exist and `static/flags/` should
    contain 271 .svg files. Remove `static/.gitkeep` if it exists (no longer needed).

    Step 2 — Add flag enrichment to `_qso_to_view_dict()` in `app/qso/ui_router.py`:

    Add imports at the top of the file (with the other imports):
    ```python
    from app.callsign.prefixes import lookup_prefix
    import pycountry
    ```

    Add flag enrichment at the end of `_qso_to_view_dict()`, just before `return d`:
    ```python
    # Flag enrichment — render-time lookup, not stored
    iso = lookup_prefix(qso.CALL) if qso.CALL else None
    d["flag_iso"] = iso.lower() if iso else None
    country_obj = pycountry.countries.get(alpha_2=iso) if iso else None
    d["flag_country"] = country_obj.name if country_obj else (iso if iso else None)
    ```

    IMPORTANT details:
    - `.lower()` on iso is REQUIRED — lookup_prefix returns uppercase "US" but flag files are lowercase "us.svg"
    - The `country_obj` guard handles Kosovo (XK) where pycountry returns None — fallback shows "XK" as tooltip
    - Do NOT add flag logic to any route handler — _qso_to_view_dict() is the single injection point for all 4 render paths
    - Do NOT touch feed_row.html or its data path — out of scope
  </action>
  <verify>
    1. `ls static/flags/ | wc -l` returns 271
    2. `ls app/static/flags/ 2>&1` returns "No such file or directory"
    3. `python -c "from app.qso.ui_router import _qso_to_view_dict; print('import ok')"` succeeds (no import errors)
    4. Existing tests still pass: `python -m pytest tests/ -x -q`
  </verify>
  <done>
    Flag SVGs are at static/flags/ (served by StaticFiles mount). _qso_to_view_dict() returns flag_iso (lowercase str or None) and flag_country (country name str or None) for every QSO.
  </done>
</task>

<task type="auto">
  <name>Task 2: Render conditional flag img in QSO row template</name>
  <files>
    templates/log/qso_row.html
  </files>
  <action>
    Modify the CALL `<td>` in `templates/log/qso_row.html` (currently line 3: `<td>{{ qso.CALL }}</td>`)
    to include a conditional flag image before the callsign text:

    ```html
    <td>
      {% if qso.flag_iso %}
      <img src="/static/flags/{{ qso.flag_iso }}.svg"
           width="20" height="15"
           alt="{{ qso.flag_country or '' }}"
           title="{{ qso.flag_country or '' }}"
           style="vertical-align:middle;margin-right:4px;">
      {% endif %}
      {{ qso.CALL }}
    </td>
    ```

    Details:
    - width="20" height="15" preserves the 4:3 SVG ratio and fits within table row line height
    - `title` attribute provides country name tooltip on hover — zero JavaScript
    - `alt` attribute uses same country name for accessibility
    - `vertical-align:middle` aligns flag with callsign text baseline
    - `margin-right:4px` adds spacing between flag and callsign
    - When flag_iso is None (unresolvable callsign), the entire img tag is omitted — no broken image
    - Do NOT modify qso_row_edit.html — edit mode does not need the flag
    - Do NOT modify qso_result.html — success message flag is out of scope for FLAG-01/FLAG-02
  </action>
  <verify>
    1. `grep -c 'flag_iso' templates/log/qso_row.html` returns at least 1
    2. `grep 'static/flags' templates/log/qso_row.html` shows the img src pattern
    3. Start the app (`python -m uvicorn app.main:app`) and verify:
       - Navigate to /log/view — flags appear next to callsigns with known prefixes
       - Hover a flag to see country name tooltip
       - Callsigns with no resolvable prefix show no flag, no broken image icon
       - Click pagination (Next/Previous) — flags persist correctly on HTMX swap
  </verify>
  <done>
    Flag img tags render conditionally in QSO log rows. Resolvable callsigns show flag + tooltip. Unresolvable callsigns show no flag, no error. HTMX pagination does not break flag rendering.
  </done>
</task>

</tasks>

<verification>
1. Static file serving: `curl -s -o /dev/null -w "%{http_code}" http://localhost:8000/static/flags/us.svg` returns 200
2. Flag visible in HTML: View page source of /log/view — `<img src="/static/flags/` appears in QSO rows with resolvable callsigns
3. No broken images: Browser DevTools Network tab shows no 404s for flag requests
4. Tooltip works: Hover any flag icon — browser shows country name
5. HTMX pagination: Click Next, verify flags still render; click Previous, verify flags still render
6. Graceful fallback: If log contains a callsign with unknown prefix, that row shows no img tag at all
7. All existing tests pass: `python -m pytest tests/ -x -q`
</verification>

<success_criteria>
- FLAG-01 met: QSO rows in the log table display flag img icons next to callsigns when prefix resolves to valid ISO alpha-2 code
- FLAG-02 met: QSO rows where prefix does not resolve display no flag — no error, no broken image
- Flags survive HTMX pagination without disappearing
- Country name tooltip on hover via title attribute
- All existing tests pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/12-flag-display-integration/12-01-SUMMARY.md`
</output>
