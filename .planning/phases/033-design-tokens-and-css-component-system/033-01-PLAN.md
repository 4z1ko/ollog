---
phase: 033-design-tokens-and-css-component-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tailwind.config.js
  - static/css/input.css
  - templates/base.html
autonomous: true

must_haves:
  truths:
    - "tailwind.config.js defines canvas.light (#f2f2f7), canvas.dark (#0f0f0f), surface.light (#ffffff), surface.dark (#1c1c1e) color tokens"
    - "tailwind.config.js defines boxShadow.card two-layer value"
    - "tailwind.config.js fontFamily.sans leads with -apple-system, BlinkMacSystemFont — no Inter entry"
    - "base.html contains no Google Fonts link tags"
    - "input.css @layer base html rule uses -apple-system, BlinkMacSystemFont font stack — no Inter"
    - ".card component class uses bg-surface-light dark:bg-surface-dark shadow-card dark:shadow-none"
    - ".table-wrap component class uses shadow-card dark:shadow-none"
    - "All four badge classes use rounded-md not rounded-full"
    - ".card-title uses text-sm font-semibold text-gray-700 dark:text-gray-200 — no uppercase, no tracking-wider"
  artifacts:
    - path: "tailwind.config.js"
      provides: "Canvas, surface, and shadow design tokens"
      contains: "canvas"
    - path: "static/css/input.css"
      provides: "Updated component class library"
      contains: "surface-light"
    - path: "templates/base.html"
      provides: "HTML shell without CDN font requests"
  key_links:
    - from: "tailwind.config.js boxShadow.card"
      to: "static/css/input.css .card @apply shadow-card"
      via: "Tailwind @apply resolution at build time"
      pattern: "shadow-card"
    - from: "tailwind.config.js colors.surface"
      to: "static/css/input.css .card @apply bg-surface-light"
      via: "Tailwind @apply resolution at build time"
      pattern: "surface-light"
---

<objective>
Define Apple-calibrated design tokens in tailwind.config.js and update the component class library in input.css to consume them. Remove the Google Fonts CDN from base.html.

Purpose: Establish the token foundation that templates will reference, and update the CSS component classes to match Apple HIG visual conventions (DSGN-01 through DSGN-05).
Output: Revised tailwind.config.js with color and shadow tokens, revised input.css with updated .card, .badge-*, .card-title, and html base rules, and a base.html with no CDN font requests.
</objective>

<execution_context>
@/Users/royco/.claude/get-shit-done/workflows/execute-plan.md
@/Users/royco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/033-design-tokens-and-css-component-system/033-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add design tokens to tailwind.config.js and remove Inter from font stack</name>
  <files>tailwind.config.js</files>
  <action>
Rewrite tailwind.config.js completely with these changes — preserve the sidebar color block exactly as-is:

1. Under theme.extend.colors, add canvas and surface token objects alongside the existing sidebar block:
   - canvas: { light: '#f2f2f7', dark: '#0f0f0f' }
   - surface: { light: '#ffffff', dark: '#1c1c1e' }

2. Under theme.extend, add a boxShadow block:
   - card: '0 1px 3px rgba(0,0,0,0.06), 0 1px 2px rgba(0,0,0,0.04)'

3. Replace the fontFamily.sans array — remove Inter entirely, lead with system fonts:
   - sans: ['-apple-system', 'BlinkMacSystemFont', '"Segoe UI"', 'Roboto', 'Helvetica', 'Arial', 'sans-serif']

The final tailwind.config.js must look like:

```js
/** @type {import('tailwindcss').Config} */
module.exports = {
  darkMode: 'class',
  content: ['./templates/**/*.html'],
  theme: {
    extend: {
      colors: {
        sidebar: {
          DEFAULT: '#1a1d2e',
          hover: '#252941',
          active: '#4338ca',
          border: '#2d3561',
          text: '#c4c9e4',
        },
        canvas: {
          light: '#f2f2f7',
          dark: '#0f0f0f',
        },
        surface: {
          light: '#ffffff',
          dark: '#1c1c1e',
        },
      },
      fontFamily: {
        sans: ['-apple-system', 'BlinkMacSystemFont', '"Segoe UI"', 'Roboto', 'Helvetica', 'Arial', 'sans-serif'],
      },
      boxShadow: {
        card: '0 1px 3px rgba(0,0,0,0.06), 0 1px 2px rgba(0,0,0,0.04)',
      },
    },
  },
  plugins: [],
}
```

Do NOT add Inter anywhere. Do NOT remove the sidebar block.
  </action>
  <verify>node -e "const c = require('./tailwind.config.js'); console.log(JSON.stringify({canvas: c.theme.extend.colors.canvas, surface: c.theme.extend.colors.surface, shadow: c.theme.extend.boxShadow, font: c.theme.extend.fontFamily.sans[0]}))"</verify>
  <done>Output shows canvas.light="#f2f2f7", surface.dark="#1c1c1e", shadow.card contains "rgba", font[0]="-apple-system". No "Inter" in fontFamily.sans array.</done>
</task>

<task type="auto">
  <name>Task 2: Update input.css component classes and html base font rule</name>
  <files>static/css/input.css</files>
  <action>
Make five targeted edits to static/css/input.css. Do NOT touch any other component classes (.btn-*, .form-input, .nav-item, .alert-*, .data-table th, .data-table td, .form-label, or legacy aliases).

**Edit 1 — @layer base html rule (line 7):**
Replace:
  font-family: 'Inter', ui-sans-serif, system-ui, -apple-system, sans-serif;
With:
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;

**Edit 2 — .card (line 74):**
Replace:
  @apply bg-white dark:bg-gray-900 rounded-xl border border-gray-200 dark:border-gray-800 shadow-sm;
With:
  @apply bg-surface-light dark:bg-surface-dark rounded-xl border border-gray-200 dark:border-gray-800 shadow-card dark:shadow-none;

**Edit 3 — .table-wrap (line 105):**
Replace:
  @apply overflow-x-auto rounded-xl border border-gray-200 dark:border-gray-800 shadow-sm;
With:
  @apply overflow-x-auto rounded-xl border border-gray-200 dark:border-gray-800 shadow-card dark:shadow-none;

**Edit 4 — .card-title (line 80):**
Replace:
  @apply text-sm font-semibold text-gray-500 dark:text-gray-400 uppercase tracking-wider;
With:
  @apply text-sm font-semibold text-gray-700 dark:text-gray-200;

**Edit 5 — All four badge classes (lines 137, 141, 145, 149):**
In each of .badge-green, .badge-red, .badge-blue, .badge-gray, change rounded-full to rounded-md.

After edits, the four badge @apply lines must read:
  .badge-green { @apply inline-flex items-center px-2 py-0.5 rounded-md text-xs font-medium bg-emerald-100 dark:bg-emerald-900/40 text-emerald-700 dark:text-emerald-400; }
  .badge-red   { @apply inline-flex items-center px-2 py-0.5 rounded-md text-xs font-medium bg-rose-100 dark:bg-rose-900/40 text-rose-700 dark:text-rose-400; }
  .badge-blue  { @apply inline-flex items-center px-2 py-0.5 rounded-md text-xs font-medium bg-indigo-100 dark:bg-indigo-900/40 text-indigo-700 dark:text-indigo-400; }
  .badge-gray  { @apply inline-flex items-center px-2 py-0.5 rounded-md text-xs font-medium bg-gray-100 dark:bg-gray-800 text-gray-600 dark:text-gray-400; }

Confirm no "rounded-full" appears in any badge class. Confirm no "Inter" appears in @layer base.
  </action>
  <verify>grep -n "rounded-full\|Inter\|bg-gray-900\|shadow-sm\|uppercase tracking-wider" /Users/royco/ollog/static/css/input.css | grep -v "form-label\|data-table\|btn\|form-input" || echo "Checks passed (no unexpected matches)"</verify>
  <done>grep finds no "rounded-full" in badge classes, no "Inter" in @layer base html rule, no "bg-gray-900" in .card, no "shadow-sm" in .card or .table-wrap, no "uppercase tracking-wider" in .card-title. (form-label and data-table th still keep their uppercase tracking-wider — those are intentional and should not be changed.)</done>
</task>

<task type="auto">
  <name>Task 3: Remove Google Fonts CDN links from base.html</name>
  <files>templates/base.html</files>
  <action>
Remove the three Google Fonts link tags from templates/base.html. These are currently on lines 34-36:

```html
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
```

Delete all three lines. The line immediately following them (`<link rel="stylesheet" href="/static/css/output.css">`) must remain untouched.

After deletion, verify that no "fonts.googleapis.com" or "fonts.gstatic.com" or "Inter" references remain in base.html.
  </action>
  <verify>grep -c "fonts.googleapis\|fonts.gstatic\|Inter" /Users/royco/ollog/templates/base.html || echo "0"</verify>
  <done>grep returns 0 — no CDN font references remain in base.html.</done>
</task>

</tasks>

<verification>
After all three tasks complete, run:

```bash
cd /Users/royco/ollog && npm run build 2>&1 | tail -5
```

Build must succeed (exit 0, no "Cannot apply unknown utility class" errors).

Then verify output.css contains the tokens:
```bash
grep -q '1c1c1e' /Users/royco/ollog/static/css/output.css && echo "surface dark: OK" || echo "surface dark: MISSING"
grep -q 'f2f2f7' /Users/royco/ollog/static/css/output.css && echo "canvas light: OK" || echo "canvas light: MISSING"
grep -q '1px 3px rgba' /Users/royco/ollog/static/css/output.css && echo "shadow-card: OK" || echo "shadow-card: MISSING"
grep -q 'apple-system' /Users/royco/ollog/static/css/output.css && echo "system font: OK" || echo "system font: MISSING"
grep -q 'Inter' /Users/royco/ollog/static/css/output.css && echo "ERROR: Inter present" || echo "Inter removed: OK"
grep 'badge-' /Users/royco/ollog/static/css/output.css | grep -q 'rounded-full' && echo "ERROR: badge still pill" || echo "badge shape rounded-md: OK"
```
</verification>

<success_criteria>
- npm run build exits 0 with no errors
- output.css contains "#1c1c1e" (surface dark token)
- output.css contains shadow value "1px 3px rgba" (shadow-card)
- output.css contains "-apple-system" font stack
- output.css does NOT contain "Inter"
- No badge class in output.css uses "rounded-full"
- base.html has no Google Fonts link tags
</success_criteria>

<output>
After completion, create `.planning/phases/033-design-tokens-and-css-component-system/033-01-SUMMARY.md`
</output>
