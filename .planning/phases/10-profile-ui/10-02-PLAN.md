---
phase: 10-profile-ui
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - templates/log/form.html
  - templates/log/log.html
  - templates/log/import.html
autonomous: true

must_haves:
  truths:
    - "Profile page is accessible via navigation link in the log UI — operators do not need to type the URL directly"
    - "Profile nav link appears consistently in all three log UI pages (form, log view, import)"
  artifacts:
    - path: "templates/log/form.html"
      provides: "Profile nav link in QSO entry page"
      contains: "/log/profile"
    - path: "templates/log/log.html"
      provides: "Profile nav link in log view page"
      contains: "/log/profile"
    - path: "templates/log/import.html"
      provides: "Profile nav link in import page"
      contains: "/log/profile"
  key_links:
    - from: "templates/log/form.html"
      to: "/log/profile"
      via: "anchor tag in nav"
      pattern: "href.*/log/profile"
    - from: "templates/log/log.html"
      to: "/log/profile"
      via: "anchor tag in nav"
      pattern: "href.*/log/profile"
    - from: "templates/log/import.html"
      to: "/log/profile"
      via: "anchor tag in nav"
      pattern: "href.*/log/profile"
---

<objective>
Add a "Profile" navigation link to all three log UI templates so operators can reach the profile settings page without typing the URL.

Purpose: Completes the UI discoverability requirement (UI-03) — profile is reachable from any log page.
Output: Modified nav sections in form.html, log.html, and import.html.
</objective>

<execution_context>
@/Users/royco/.claude/get-shit-done/workflows/execute-plan.md
@/Users/royco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@templates/log/form.html
@templates/log/log.html
@templates/log/import.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Profile nav link to form.html, log.html, and import.html</name>
  <files>templates/log/form.html, templates/log/log.html, templates/log/import.html</files>
  <action>
In each of the three templates, add `<a href="/log/profile">Profile</a>` to the nav bar between the Export link and the Logout link. The placement should be consistent across all three files.

**templates/log/form.html** — current nav links are: Log View, Import, Export, Logout. Change to:
```
Log View | Import | Export | Profile | Logout
```
Insert `<a href="/log/profile">Profile</a>` immediately before the `<a href="/log/logout">Logout</a>` line.

**templates/log/log.html** — current nav links are: Log QSO, Import, Export, Logout. Change to:
```
Log QSO | Import | Export | Profile | Logout
```
Insert `<a href="/log/profile">Profile</a>` immediately before the `<a href="/log/logout">Logout</a>` line.

**templates/log/import.html** — current nav links are: Log QSO, Log View, Logout. Change to:
```
Log QSO | Log View | Export | Profile | Logout
```
Insert `<a href="/log/export">Export</a>` and `<a href="/log/profile">Profile</a>` immediately before the `<a href="/log/logout">Logout</a>` line. (Import page was missing Export link — add it for consistency.)

Note: profile.html (created in 10-01) already has the Profile link in its nav, so no change needed there.
  </action>
  <verify>
Grep all three files for `/log/profile` — each must contain the link:
```
grep -l "/log/profile" templates/log/form.html templates/log/log.html templates/log/import.html
```
Should return all three files.
  </verify>
  <done>All three log UI templates (form.html, log.html, import.html) contain a "Profile" nav link pointing to /log/profile, placed consistently between Export and Logout.</done>
</task>

</tasks>

<verification>
1. Navigate to /log/ (QSO form) — "Profile" link visible in nav between Export and Logout
2. Navigate to /log/view (log view) — "Profile" link visible in nav between Export and Logout
3. Navigate to /log/import (import page) — "Profile" link visible in nav between Export and Logout
4. Click the Profile link from any page — lands on /log/profile
</verification>

<success_criteria>
- "Profile" nav link appears in all three log UI pages (form.html, log.html, import.html)
- Link placement is consistent: between Export and Logout
- Clicking the link navigates to /log/profile
</success_criteria>

<output>
After completion, create `.planning/phases/10-profile-ui/10-02-SUMMARY.md`
</output>
