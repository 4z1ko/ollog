---
phase: 06-navigation-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/log/form.html
  - templates/log/log.html
autonomous: true

must_haves:
  truths:
    - "templates/log/form.html nav bar contains links to /log/import and /log/export"
    - "templates/log/log.html nav bar contains links to /log/import and /log/export"
    - "Operators can click Import and Export from either the QSO form or log view pages"
  artifacts:
    - path: "templates/log/form.html"
      provides: "Nav with Import and Export links"
      contains: "/log/import"
    - path: "templates/log/log.html"
      provides: "Nav with Import and Export links"
      contains: "/log/export"
  key_links:
    - from: "templates/log/form.html"
      to: "app/qso/ui_router.py"
      via: "nav links to /log/import and /log/export"
      pattern: "log/import"
    - from: "templates/log/log.html"
      to: "app/qso/ui_router.py"
      via: "nav links to /log/import and /log/export"
      pattern: "log/export"
---

<objective>
Add navigation links to the ADIF import and export pages in the operator log UI templates.

Purpose: The import (/log/import) and export (/log/export) pages are fully implemented but unreachable by navigation — operators must type URLs directly. Adding two links to two nav bars closes this discoverability gap.
Output: Updated form.html and log.html nav bars with Import and Export links.
</objective>

<execution_context>
@/Users/royco/.claude/get-shit-done/workflows/execute-plan.md
@/Users/royco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@templates/log/form.html
@templates/log/log.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Import and Export nav links to both log templates</name>
  <files>templates/log/form.html, templates/log/log.html</files>
  <action>
**In `templates/log/form.html`**, update the nav div (lines 5-9) to add Import and Export links:

Current:
```html
  <div style="display:flex;gap:1rem;align-items:center;">
    <span>Logged in as <strong>{{ callsign }}</strong></span>
    <a href="/log/view">Log View</a>
    <a href="/log/logout">Logout</a>
  </div>
```

Replace with:
```html
  <div style="display:flex;gap:1rem;align-items:center;">
    <span>Logged in as <strong>{{ callsign }}</strong></span>
    <a href="/log/view">Log View</a>
    <a href="/log/import">Import</a>
    <a href="/log/export">Export</a>
    <a href="/log/logout">Logout</a>
  </div>
```

**In `templates/log/log.html`**, update the nav div (lines 5-9) to add Import and Export links:

Current:
```html
  <div style="display:flex;gap:1rem;align-items:center;">
    <span>Logged in as <strong>{{ callsign }}</strong></span>
    <a href="/log/">Log QSO</a>
    <a href="/log/logout">Logout</a>
  </div>
```

Replace with:
```html
  <div style="display:flex;gap:1rem;align-items:center;">
    <span>Logged in as <strong>{{ callsign }}</strong></span>
    <a href="/log/">Log QSO</a>
    <a href="/log/import">Import</a>
    <a href="/log/export">Export</a>
    <a href="/log/logout">Logout</a>
  </div>
```

Keep all other content in both files completely unchanged.
  </action>
  <verify>
Run:
```bash
grep -n "log/import\|log/export" templates/log/form.html templates/log/log.html
```
Both files must contain both `/log/import` and `/log/export`.
  </verify>
  <done>Both nav bars contain Import and Export links. Operators can navigate to import/export pages from both the QSO form and log view.</done>
</task>

</tasks>

<verification>
- `grep "log/import" templates/log/form.html templates/log/log.html` returns matches in both files
- `grep "log/export" templates/log/form.html templates/log/log.html` returns matches in both files
- No other content changed in either file
</verification>

<success_criteria>
Both log UI templates have Import and Export nav links. The ADIF import/export E2E flows are now fully navigable without direct URL entry.
</success_criteria>

<output>
After completion, create `.planning/phases/06-navigation-fix/06-01-SUMMARY.md`
</output>
