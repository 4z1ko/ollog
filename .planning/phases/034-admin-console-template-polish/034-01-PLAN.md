---
phase: 034-admin-console-template-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/base_app.html
  - templates/admin/users.html
  - templates/admin/users_table.html
autonomous: true

must_haves:
  truths:
    - "Admin sidebar icons are 24px (w-6 h-6) in both the nav link and logout link"
    - "Admin sidebar background in dark mode uses #1c1c1e (dark:bg-surface-dark), not #1a1d2e"
    - "The toggle button (Disable/Enable) has an aria-label identifying the action and the specific operator username"
    - "The reset password submit button has an aria-label identifying the action and the specific operator username"
    - "The toggle button displays a w-4 h-4 SVG icon appropriate to the action (no-symbol for Disable, check-circle for Enable)"
    - "The reset password button displays a w-4 h-4 key SVG icon"
  artifacts:
    - path: "templates/base_app.html"
      provides: "sidebar_class block extension point on the <aside> element"
      contains: "{% block sidebar_class %}{% endblock %}"
    - path: "templates/admin/users.html"
      provides: "dark:bg-surface-dark literal class string for Tailwind purge + w-6 h-6 nav icons"
      contains: "dark:bg-surface-dark"
    - path: "templates/admin/users_table.html"
      provides: "aria-label attributes and SVG icons on all action buttons"
      contains: "aria-label"
  key_links:
    - from: "templates/admin/users.html sidebar_class block"
      to: "templates/base_app.html <aside> class list"
      via: "{% block sidebar_class %} Jinja2 block override"
      pattern: "block sidebar_class"
    - from: "dark:bg-surface-dark in users.html"
      to: "Tailwind purge scanner"
      via: "literal class string in scanned template"
      pattern: "dark:bg-surface-dark"
---

<objective>
Implement ADMN-02 and ADMN-03: update the admin sidebar to use `#1c1c1e` dark mode background and correct 24px nav icons, and add accessible icons and aria-labels to operator action buttons.

Purpose: Complete the three outstanding gaps identified in research — sidebar icon sizing, sidebar dark background, and action button accessibility. ADMN-01 is already satisfied; this plan closes ADMN-02 and ADMN-03.
Output: Three edited template files with no Python changes.
</objective>

<execution_context>
@/Users/royco/.claude/get-shit-done/workflows/execute-plan.md
@/Users/royco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/034-admin-console-template-polish/034-RESEARCH.md
@templates/base_app.html
@templates/admin/users.html
@templates/admin/users_table.html
@tailwind.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin sidebar — dark:bg-surface-dark block and w-6 h-6 icons</name>
  <files>templates/base_app.html, templates/admin/users.html</files>
  <action>
    In `templates/base_app.html`, locate the `<aside id="sidebar"` opening tag (line ~17). Its class attribute currently reads:
    `class="fixed inset-y-0 left-0 z-30 w-64 bg-sidebar flex flex-col`

    Insert `{% block sidebar_class %}{% endblock %}` BETWEEN `bg-sidebar` and `flex flex-col`, preserving the surrounding whitespace exactly:
    `class="fixed inset-y-0 left-0 z-30 w-64 bg-sidebar {% block sidebar_class %}{% endblock %} flex flex-col`

    The default block is empty — no operator templates are affected. The rest of the aside tag (the translate/transition/md: classes on the continuation lines) must remain unchanged.

    In `templates/admin/users.html`:

    1. Add a new block override after the closing `{% endblock %}` of `sidebar_user` (around line 28) and BEFORE `{% block sidebar_logout %}`. Add it as a new top-level block at the start of the file, immediately after `{% block active_page %}users{% endblock %}` (around line 3), so it is adjacent to the other block overrides but clearly separated:

       ```
       {% block sidebar_class %}dark:bg-surface-dark{% endblock %}
       ```

       Place it at line 5, between `{% block active_page %}users{% endblock %}` and the `{# Override sidebar for admin area #}` comment. The literal string `dark:bg-surface-dark` must appear in this file for Tailwind's content scanner to include the utility in output.css.

    2. In `{% block sidebar_nav %}` (around line 9): change the SVG's `class="w-5 h-5 flex-shrink-0"` to `class="w-6 h-6 flex-shrink-0"`. This is the Operators nav link icon.

    3. In `{% block sidebar_logout %}` (around line 32): change the SVG's `class="w-5 h-5 flex-shrink-0"` to `class="w-6 h-6 flex-shrink-0"`. This is the Logout link icon.

    Do NOT touch the `sidebar_user` block — the shield icon inside the avatar badge at `w-4 h-4` is correct badge-icon sizing, not a nav icon.

    Do NOT add `transition-colors` or any color transition to the aside — the transition flash rule prohibits color transitions on the sidebar shell.
  </action>
  <verify>
    Run: `grep -n "sidebar_class" /Users/royco/ollog/templates/base_app.html`
    Expected: one hit showing `{% block sidebar_class %}{% endblock %}` inside the aside class attribute.

    Run: `grep -n "sidebar_class\|dark:bg-surface-dark" /Users/royco/ollog/templates/admin/users.html`
    Expected: two hits — the block definition and the literal `dark:bg-surface-dark` string.

    Run: `grep -n "w-6 h-6" /Users/royco/ollog/templates/admin/users.html`
    Expected: two hits — one in sidebar_nav SVG and one in sidebar_logout SVG.

    Run: `grep -n "w-5 h-5" /Users/royco/ollog/templates/admin/users.html`
    Expected: zero hits (all w-5 h-5 must be replaced with w-6 h-6 in this file).
  </verify>
  <done>
    `base_app.html` has `{% block sidebar_class %}{% endblock %}` in the `<aside>` class attribute. `users.html` has the `sidebar_class` block emitting `dark:bg-surface-dark` as a literal string, and both admin sidebar SVG icons use `w-6 h-6`. No `w-5 h-5` remains in `users.html`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Action button icons and aria-labels in users_table.html</name>
  <files>templates/admin/users_table.html</files>
  <action>
    In `templates/admin/users_table.html`, replace the action cell content (the `<div class="flex flex-wrap items-center gap-2">` block, currently lines 44–61) with the following updated version.

    The toggle button (currently text-only, no aria-label) must become:

    ```html
    <button
      hx-post="/admin/ui/users/{{ user.username }}/toggle"
      hx-target="#users-table-body"
      hx-swap="innerHTML"
      aria-label="{{ 'Disable' if user.enabled else 'Enable' }} operator {{ user.username }}"
      class="{{ 'btn-danger' if user.enabled else 'btn-success' }} btn-sm"
    >
      {% if user.enabled %}
      <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" aria-hidden="true">
        <path stroke-linecap="round" stroke-linejoin="round" d="M18.364 18.364A9 9 0 0 0 5.636 5.636m12.728 12.728A9 9 0 0 1 5.636 5.636m12.728 12.728L5.636 5.636" />
      </svg>
      Disable
      {% else %}
      <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" aria-hidden="true">
        <path stroke-linecap="round" stroke-linejoin="round" d="M9 12.75 11.25 15 15 9.75M21 12a9 9 0 1 1-18 0 9 9 0 0 1 18 0Z" />
      </svg>
      Enable
      {% endif %}
    </button>
    ```

    Icon choices (Heroicons outline v2):
    - Disable: `no-symbol` path (circle with diagonal slash) — `d="M18.364 18.364A9 9 0 0 0 5.636 5.636m12.728 12.728A9 9 0 0 1 5.636 5.636m12.728 12.728L5.636 5.636"`
    - Enable: `check-circle` path — `d="M9 12.75 11.25 15 15 9.75M21 12a9 9 0 1 1-18 0 9 9 0 0 1 18 0Z"` (this same path already appears in the alert-success block at line 18 — reuse it for consistency)

    The reset password submit button (currently text-only `Reset`, no aria-label) must become:

    ```html
    <button type="submit"
            aria-label="Reset password for {{ user.username }}"
            class="btn-secondary btn-sm">
      <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" aria-hidden="true">
        <path stroke-linecap="round" stroke-linejoin="round" d="M15.75 5.25a3 3 0 0 1 3 3m3 0a6 6 0 0 1-7.029 5.912c-.563-.097-1.159.026-1.563.43L10.5 17.25H8.25v2.25H6v2.25H2.25v-2.818c0-.597.237-1.17.659-1.591l6.499-6.499c.404-.404.527-1 .43-1.563A6 6 0 0 1 21.75 8.25Z" />
      </svg>
      Reset
    </button>
    ```

    Icon: Heroicons outline `key` — `d="M15.75 5.25a3 3 0 0 1 3 3m3 0a6 6 0 0 1-7.029 5.912c-.563-.097-1.159.026-1.563.43L10.5 17.25H8.25v2.25H6v2.25H2.25v-2.818c0-.597.237-1.17.659-1.591l6.499-6.499c.404-.404.527-1 .43-1.563A6 6 0 0 1 21.75 8.25Z"`

    All SVG icons must have `aria-hidden="true"` — the aria-label on the button itself is the accessible name; the icon is decorative.

    The surrounding form structure (hx-post, hx-target, hx-swap, class="form-input w-36" password input) remains unchanged.
  </action>
  <verify>
    Run: `grep -n "aria-label" /Users/royco/ollog/templates/admin/users_table.html`
    Expected: two hits — one on the toggle button and one on the reset submit button.

    Run: `grep -n "aria-hidden" /Users/royco/ollog/templates/admin/users_table.html`
    Expected: three hits — one on the Disable icon, one on the Enable icon, one on the Reset icon.

    Run: `grep -n "w-4 h-4" /Users/royco/ollog/templates/admin/users_table.html`
    Expected: at least 5 hits (3 new action button icons + 2 pre-existing alert icons in the error/success rows).
  </verify>
  <done>
    The toggle button has `aria-label="{{ 'Disable' if user.enabled else 'Enable' }} operator {{ user.username }}"` and conditional `w-4 h-4` SVG icons for both states. The reset submit button has `aria-label="Reset password for {{ user.username }}"` and a `w-4 h-4` key icon. All three new SVGs have `aria-hidden="true"`.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, confirm:
1. `grep "sidebar_class" templates/base_app.html` — returns the Jinja2 block inside the aside class attribute
2. `grep "dark:bg-surface-dark" templates/admin/users.html` — returns the literal class string (required for Tailwind purge)
3. `grep "w-5 h-5" templates/admin/users.html` — returns zero results
4. `grep "aria-label" templates/admin/users_table.html` — returns two results (toggle + reset buttons)
5. Operator templates (`base.html`, `log/*.html`) are not affected — the empty default `sidebar_class` block adds nothing to operator sidebar appearance
</verification>

<success_criteria>
- ADMN-02: Admin sidebar `<aside>` has `{% block sidebar_class %}` extension point; `users.html` emits `dark:bg-surface-dark`; both admin nav icons are `w-6 h-6`
- ADMN-03: Toggle button and reset button both have `aria-label` with action + username; all three action button icon states have `w-4 h-4` SVGs with `aria-hidden="true"`
- No Python files touched, no structural changes to table/card layout in `users.html`
</success_criteria>

<output>
After completion, create `.planning/phases/034-admin-console-template-polish/034-01-SUMMARY.md`
</output>
