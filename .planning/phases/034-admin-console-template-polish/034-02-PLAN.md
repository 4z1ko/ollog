---
phase: 034-admin-console-template-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 034-01
files_modified:
  - static/css/output.css
autonomous: false

must_haves:
  truths:
    - "npm run build completes without errors"
    - "dark:bg-surface-dark rule appears in output.css with the correct #1c1c1e color value"
    - "Admin sidebar visually shows #1c1c1e background in dark mode (user-verified)"
    - "Admin sidebar nav and logout icons are 24px in the rendered UI (user-verified)"
    - "Operator action buttons show icons and have aria-label text in browser devtools"
  artifacts:
    - path: "static/css/output.css"
      provides: "Compiled Tailwind CSS including dark:bg-surface-dark utility"
      contains: "dark\\:bg-surface-dark"
  key_links:
    - from: "dark:bg-surface-dark literal string in templates/admin/users.html"
      to: "static/css/output.css compiled rule"
      via: "npm run build Tailwind content scanner"
      pattern: "dark\\\\:bg-surface-dark"
---

<objective>
Build the Tailwind CSS bundle and verify all three ADMN requirements are visually correct.

Purpose: `dark:bg-surface-dark` is not in output.css until a build runs after the template change. This plan rebuilds output.css and gates on human visual verification of the admin UI.
Output: Rebuilt output.css with `dark:bg-surface-dark` compiled, and user confirmation that the admin UI matches requirements.
</objective>

<execution_context>
@/Users/royco/.claude/get-shit-done/workflows/execute-plan.md
@/Users/royco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/034-admin-console-template-polish/034-01-SUMMARY.md
@tailwind.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build CSS and verify dark:bg-surface-dark in output</name>
  <files>static/css/output.css</files>
  <action>
    From the project root (`/Users/royco/ollog`), run:

    ```
    npm run build
    ```

    After the build completes, verify the new utility rule is present:

    ```
    grep "surface-dark" static/css/output.css
    ```

    Expected output: a line containing `dark\:bg-surface-dark` with a CSS rule using `rgb(28 28 30` as the background-color value (matching `surface.dark: '#1c1c1e'` in tailwind.config.js).

    If the grep returns no results:
    1. Check that `templates/admin/users.html` contains the literal string `dark:bg-surface-dark` (not split across lines or wrapped in a Jinja expression)
    2. Check that `tailwind.config.js` content array includes `"templates/**/*.html"`
    3. Re-run `npm run build` and grep again

    Also verify no regressions — the existing classes used in admin templates must still be present:
    - `grep "bg-sidebar" static/css/output.css` — must return at least one hit
    - `grep "btn-danger\|btn-success\|btn-secondary\|btn-sm" static/css/output.css` — must return hits

    Do NOT commit output.css — it is a build artifact.
  </action>
  <verify>
    `grep "surface-dark" static/css/output.css` returns a line with `dark\:bg-surface-dark` and `28 28 30` color values.
    `grep "bg-sidebar" static/css/output.css` returns at least one hit (no regression).
    `npm run build` exits with code 0.
  </verify>
  <done>
    output.css is rebuilt and contains the `dark:bg-surface-dark` utility compiled from the `surface.dark: '#1c1c1e'` token. No prior utility classes are missing.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual verification of admin UI changes</name>
  <files>templates/admin/users.html, templates/base_app.html, templates/admin/users_table.html</files>
  <action>
    PAUSE — human visual verification required. The automated build is complete. Verify the rendered admin UI matches all ADMN requirements before marking this phase done.

    What was built in plan 034-01:
    1. Admin sidebar dark mode background: `<aside>` now applies `dark:bg-surface-dark` (#1c1c1e) in dark mode via the new sidebar_class block override in users.html
    2. Admin sidebar icons bumped from 20px to 24px: Operators nav icon and Logout icon both use w-6 h-6
    3. Operator action buttons: toggle (Disable/Enable) and reset password submit both have w-4 h-4 SVG icons and aria-label attributes

    Verification steps — visit http://localhost:8001/admin/ui/users:

    Step 1 — Sidebar dark mode background:
    - Toggle to dark mode using the theme toggle
    - The sidebar background should be rgb(28, 28, 30) = #1c1c1e in dark mode
    - In browser devtools, inspect `<aside id="sidebar">` and confirm the computed background-color is rgb(28, 28, 30)

    Step 2 — Sidebar icon sizes:
    - The "Operators" nav link icon and the "Logout" link icon should be 24px
    - In devtools, inspect either sidebar SVG and confirm width/height computed values are 24px

    Step 3 — Action button icons and aria-labels:
    - Each operator row shows a small icon inside the Disable/Enable button (circle-slash for Disable, check-circle for Enable)
    - The Reset button shows a small key icon to the left of "Reset"
    - In devtools, confirm toggle button `aria-label` reads "Disable operator jsmith" (or Enable)
    - Confirm reset submit button `aria-label` reads "Reset password for jsmith"

    Step 4 — Operator app regression check:
    - Visit http://localhost:8000/log/ and confirm the operator sidebar is visually unchanged
  </action>
  <verify>
    Human confirms all four verification steps pass. Type "approved" if so, or describe the issue observed.
  </verify>
  <done>
    Admin UI visually matches ADMN-02 (dark sidebar #1c1c1e, 24px icons) and ADMN-03 (action button icons + aria-labels). Operator app sidebar unchanged.
  </done>
</task>

</tasks>

<verification>
Phase 034 complete when:
1. `npm run build` exits 0
2. `grep "surface-dark" static/css/output.css` returns the compiled `dark:bg-surface-dark` rule
3. Human confirms admin sidebar dark mode background is #1c1c1e
4. Human confirms admin sidebar icons are 24px
5. Human confirms operator action buttons have visible icons and correct aria-label attributes
6. Human confirms operator app sidebar is visually unchanged (no regression)
</verification>

<success_criteria>
- output.css rebuilt and contains `dark:bg-surface-dark` with rgb(28 28 30) value
- Admin UI verified by human: dark sidebar at #1c1c1e, 24px nav icons, action button icons and aria-labels present
- Operator app unaffected (empty sidebar_class block introduces no visual change)
- All three ADMN requirements confirmed satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/034-admin-console-template-polish/034-02-SUMMARY.md`
</output>
