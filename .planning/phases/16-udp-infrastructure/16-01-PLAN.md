---
phase: 16-udp-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/qso/service.py
  - app/adif/router.py
  - app/qso/ui_router.py
autonomous: true

must_haves:
  truths:
    - "process_import logic lives in app/qso/service.py, not app/adif/router.py"
    - "import_qsos_from_bytes raises ValueError on size limit, never HTTPException"
    - "API endpoint POST /api/adif/import still works identically from caller perspective"
    - "UI import endpoint still renders error div on size limit exceeded"
  artifacts:
    - path: "app/qso/service.py"
      provides: "import_qsos_from_bytes function"
      contains: "async def import_qsos_from_bytes"
    - path: "app/adif/router.py"
      provides: "Thin wrapper calling import_qsos_from_bytes"
      contains: "import_qsos_from_bytes"
    - path: "app/qso/ui_router.py"
      provides: "Updated caller catching ValueError instead of HTTPException for size guard"
      contains: "ValueError"
  key_links:
    - from: "app/adif/router.py"
      to: "app/qso/service.py"
      via: "import and call import_qsos_from_bytes"
      pattern: "from app\\.qso\\.service import.*import_qsos_from_bytes"
    - from: "app/qso/ui_router.py"
      to: "app/qso/service.py"
      via: "import and call import_qsos_from_bytes"
      pattern: "from app\\.qso\\.service import.*import_qsos_from_bytes"
    - from: "app/adif/router.py"
      to: "app/qso/service.py"
      via: "catches ValueError and re-raises as HTTPException"
      pattern: "except ValueError.*HTTPException"
---

<objective>
Extract the shared QSO import logic from app/adif/router.py into app/qso/service.py so it can be called from both the HTTP endpoint and the upcoming UDP async task.

Purpose: The current process_import() raises HTTPException, which is a FastAPI/HTTP concept and cannot be caught from a non-HTTP context (asyncio UDP task). Phase 17 needs a pure-domain function that raises ValueError instead.

Output: import_qsos_from_bytes() in app/qso/service.py; thin wrappers in both router files.
</objective>

<execution_context>
@/Users/royco/.claude/get-shit-done/workflows/execute-plan.md
@/Users/royco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/adif/router.py
@app/qso/service.py
@app/qso/ui_router.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move process_import logic to app/qso/service.py as import_qsos_from_bytes</name>
  <files>app/qso/service.py, app/adif/router.py</files>
  <action>
In app/qso/service.py, add a new async function import_qsos_from_bytes(raw: bytes, operator: str) -> dict that contains the full logic currently in process_import() from app/adif/router.py. Key changes from the original:

1. Move _REQUIRED_FIELDS and _MAX_BYTES constants from app/adif/router.py to app/qso/service.py (they belong with the domain logic now).

2. The size guard MUST raise ValueError("File exceeds 10 MB limit") instead of HTTPException. This is the critical change — ValueError is catchable from any context (HTTP, UDP, CLI).

3. Add the necessary imports at the top of app/qso/service.py: parse_adi from app.adif.parser (already exists in the project).

4. The rest of the logic (parse, required-field check, build_qso_dict, find_duplicate, QSO insert, error accumulation) stays identical — copy it faithfully. build_qso_dict and find_duplicate are already in this file.

5. In app/adif/router.py:
   - Remove the process_import function entirely
   - Remove _REQUIRED_FIELDS and _MAX_BYTES constants
   - Remove the parse_adi import (no longer needed here)
   - Remove the QSO import, build_qso_dict import, find_duplicate import (no longer needed here)
   - Add import: from app.qso.service import import_qsos_from_bytes
   - Update import_adif endpoint to call import_qsos_from_bytes(raw, operator), wrapping in try/except ValueError to re-raise as HTTPException(413, detail=str(e)). This preserves the exact same HTTP behavior.
   - Keep the export endpoint, its helpers, and response models untouched.
  </action>
  <verify>Run: cd /Users/royco/ollog && python -c "from app.qso.service import import_qsos_from_bytes; print('OK')" and python -c "from app.adif.router import router; print('OK')" — both must succeed without ImportError.</verify>
  <done>import_qsos_from_bytes exists in app/qso/service.py raising ValueError on size limit. app/adif/router.py import_adif endpoint calls it and translates ValueError to HTTPException(413).</done>
</task>

<task type="auto">
  <name>Task 2: Update UI router to call import_qsos_from_bytes and catch ValueError</name>
  <files>app/qso/ui_router.py</files>
  <action>
In app/qso/ui_router.py:

1. Change the import: replace "from app.adif.router import process_import" with "from app.qso.service import import_qsos_from_bytes".

2. At the call site (around line 482-489), change:
   - report = await process_import(raw, callsign)  -->  report = await import_qsos_from_bytes(raw, callsign)
   - except HTTPException as exc:  -->  except ValueError as exc:
   - Update the error message extraction: exc.detail  -->  str(exc)  (ValueError has no .detail attribute)

3. After this change, the HTTPException import in ui_router.py may no longer be needed for this specific handler, BUT check if HTTPException is used elsewhere in the file before removing it. If other handlers use it, keep the import.

4. Verify the HTML error div still renders: content=f'<div class="error-msg">{str(exc)}</div>'
  </action>
  <verify>Run: cd /Users/royco/ollog && python -c "from app.qso.ui_router import ui_router; print('OK')" — must succeed. Then grep for "process_import" in app/ directory — should return zero hits in .py files (only planning docs may reference it).</verify>
  <done>ui_router.py imports import_qsos_from_bytes from app.qso.service, catches ValueError instead of HTTPException for size guard, renders same error div. No remaining references to process_import in app/ Python files.</done>
</task>

</tasks>

<verification>
1. python -c "from app.qso.service import import_qsos_from_bytes" succeeds
2. python -c "from app.adif.router import router" succeeds
3. python -c "from app.qso.ui_router import ui_router" succeeds
4. grep -r "process_import" app/ --include="*.py" returns no matches
5. grep -r "HTTPException" app/qso/service.py returns no matches (service layer must not depend on FastAPI HTTP concepts)
6. grep "import_qsos_from_bytes" app/qso/service.py confirms function exists
7. grep "ValueError" app/qso/service.py confirms size guard raises ValueError
</verification>

<success_criteria>
- import_qsos_from_bytes in app/qso/service.py is the single source of QSO import logic
- It raises ValueError (not HTTPException) for size limit violations
- Both app/adif/router.py and app/qso/ui_router.py call it as thin wrappers
- app/adif/router.py translates ValueError to HTTPException(413) for HTTP callers
- app/qso/ui_router.py catches ValueError and renders HTML error div
- No process_import function remains in any Python file
- app/qso/service.py has zero FastAPI HTTP imports (no HTTPException, no status)
</success_criteria>

<output>
After completion, create `.planning/phases/16-udp-infrastructure/16-01-SUMMARY.md`
</output>
