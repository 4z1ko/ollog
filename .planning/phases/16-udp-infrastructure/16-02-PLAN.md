---
phase: 16-udp-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/config.py
  - app/udp/__init__.py
  - app/udp/server.py
  - app/main.py
  - docker-compose.yml
autonomous: true

must_haves:
  truths:
    - "UDP_ENABLED=true causes socket bind and startup banner log line"
    - "UDP_ENABLED=false (default) creates no socket, app identical to v1.3"
    - "Raw UDP datagram produces a receipt log line with source address and byte count"
    - "App shuts down cleanly — transport closed before database connection"
    - "Docker Compose exposes UDP port with /udp suffix"
  artifacts:
    - path: "app/config.py"
      provides: "UDP configuration fields"
      contains: "udp_enabled"
    - path: "app/udp/__init__.py"
      provides: "UDP package marker"
    - path: "app/udp/server.py"
      provides: "QSODatagramProtocol and start_udp_listener"
      contains: "class QSODatagramProtocol"
    - path: "app/main.py"
      provides: "Lifespan UDP startup and shutdown hooks"
      contains: "udp_transport"
    - path: "docker-compose.yml"
      provides: "UDP port mapping"
      contains: "2399:2399/udp"
  key_links:
    - from: "app/main.py"
      to: "app/udp/server.py"
      via: "conditional import and call to start_udp_listener"
      pattern: "from app\\.udp\\.server import start_udp_listener"
    - from: "app/main.py"
      to: "app/config.py"
      via: "settings.udp_enabled gate"
      pattern: "settings\\.udp_enabled"
    - from: "app/udp/server.py"
      to: "asyncio"
      via: "get_running_loop().create_datagram_endpoint"
      pattern: "get_running_loop"
    - from: "app/main.py"
      to: "app/udp/server.py"
      via: "transport.close() before close_db()"
      pattern: "udp_transport\\.close\\(\\)"
---

<objective>
Create the UDP listener skeleton: configuration, DatagramProtocol, lifespan integration, and Docker port mapping.

Purpose: Operators can enable a UDP socket, see the startup banner, send a test datagram and see a receipt log, and verify Docker UDP forwarding — all before any QSO processing logic exists (Phase 17).

Output: Working UDP listener that binds, logs, and shuts down cleanly.
</objective>

<execution_context>
@/Users/royco/.claude/get-shit-done/workflows/execute-plan.md
@/Users/royco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/config.py
@app/main.py
@docker-compose.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add UDP config fields and create UDP server module</name>
  <files>app/config.py, app/udp/__init__.py, app/udp/server.py</files>
  <action>
1. In app/config.py, add four fields to the Settings class (after the existing admin_* fields):

    udp_enabled: bool = False
    udp_port: int = 2399
    udp_bind_host: str = "127.0.0.1"
    udp_operator: str | None = None

2. Create app/udp/__init__.py as an empty file (package marker).

3. Create app/udp/server.py with:

    import asyncio
    import logging

    logger = logging.getLogger(__name__)

    class QSODatagramProtocol(asyncio.DatagramProtocol):
        """Skeleton UDP protocol for receiving QSO datagrams.

        Phase 16: logs receipt only. Phase 17 will add QSO processing.
        """

        def __init__(self) -> None:
            self.transport: asyncio.DatagramTransport | None = None
            self._background_tasks: set[asyncio.Task] = set()

        def connection_made(self, transport: asyncio.DatagramTransport) -> None:  # type: ignore[override]
            self.transport = transport

        def datagram_received(self, data: bytes, addr: tuple[str, int]) -> None:
            logger.info("UDP datagram received from %s:%s (%d bytes)", addr[0], addr[1], len(data))

        def error_received(self, exc: Exception) -> None:
            logger.warning("UDP error received: %s", exc)

        def connection_lost(self, exc: Exception | None) -> None:
            logger.info("UDP transport closed")

    async def start_udp_listener(host: str, port: int) -> tuple[asyncio.DatagramTransport, QSODatagramProtocol]:
        """Bind a UDP socket and return the transport and protocol.

        Uses asyncio.get_running_loop() (not deprecated get_event_loop()).
        """
        loop = asyncio.get_running_loop()
        transport, protocol = await loop.create_datagram_endpoint(
            QSODatagramProtocol,
            local_addr=(host, port),
        )
        logger.info("UDP listener bound to %s:%s", host, port)
        return transport, protocol  # type: ignore[return-value]

Key notes:
- Use asyncio.get_running_loop() per locked decision (Python 3.14 compatible).
- _background_tasks set is established now for Phase 17 use.
- The type: ignore comments are needed because asyncio type stubs return BaseTransport/BaseProtocol, not the Datagram subtypes.
  </action>
  <verify>Run: cd /Users/royco/ollog && python -c "from app.udp.server import QSODatagramProtocol, start_udp_listener; print('OK')" and python -c "from app.config import settings; assert settings.udp_enabled == False; assert settings.udp_port == 2399; assert settings.udp_bind_host == '127.0.0.1'; assert settings.udp_operator is None; print('OK')" — both must succeed.</verify>
  <done>Config has four UDP fields with correct defaults. QSODatagramProtocol exists with connection_made, datagram_received, error_received, connection_lost. start_udp_listener uses get_running_loop(). _background_tasks set initialized.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate UDP into lifespan and add Docker UDP port</name>
  <files>app/main.py, docker-compose.yml</files>
  <action>
1. In app/main.py lifespan function, add UDP startup AFTER the watcher_task setup and BEFORE yield. Add UDP shutdown AFTER yield and BEFORE watcher_task cancellation. The full lifespan should be:

    @asynccontextmanager
    async def lifespan(app: FastAPI):
        await init_db()
        await _bootstrap_admin()
        # Start change stream watcher for live feed
        client = get_client()
        watcher_task = None
        if client is not None:
            from app.feed.manager import manager as feed_manager, watch_qsos
            collection = client[settings.mongodb_db]["qsos"]
            watcher_task = asyncio.create_task(
                watch_qsos(collection, feed_manager, _templates)
            )
        # Start UDP listener (conditional on UDP_ENABLED)
        udp_transport = None
        if settings.udp_enabled:
            from app.udp.server import start_udp_listener
            udp_transport, _ = await start_udp_listener(
                settings.udp_bind_host, settings.udp_port
            )
        yield
        # Shutdown: UDP transport first, then watcher, then DB
        if udp_transport is not None:
            udp_transport.close()
        if watcher_task is not None:
            watcher_task.cancel()
            try:
                await watcher_task
            except asyncio.CancelledError:
                pass
        await close_db()

    CRITICAL ordering: udp_transport.close() is synchronous (not awaited). It MUST come before close_db() to avoid using DB during shutdown. Placing it before watcher_task cancellation is correct — the UDP transport does not depend on the watcher.

2. In docker-compose.yml, add the UDP port to the api service ports list:

    ports:
      - "8000:8000"
      - "2399:2399/udp"

    The /udp suffix is required — without it Docker maps TCP only.

3. Also add a comment in docker-compose.yml environment section for the api service noting that inside Docker, UDP_BIND_HOST should be 0.0.0.0 (not 127.0.0.1) for the host to reach the container:

    environment:
      - SECRET_KEY=dev-secret-change-in-production
      - MONGODB_URI=mongodb://mongodb:27017/?replicaSet=rs0
      - MONGODB_DB=ollog
      # UDP_BIND_HOST=0.0.0.0 required inside Docker (default 127.0.0.1 blocks host access)
  </action>
  <verify>
1. Run: cd /Users/royco/ollog && python -c "from app.main import app; print('OK')" — must succeed (import check).
2. Run: cd /Users/royco/ollog && grep -q "2399:2399/udp" docker-compose.yml && echo "OK" — must print OK.
3. Run: cd /Users/royco/ollog && grep -q "udp_transport" app/main.py && echo "OK" — must print OK.
4. Run: cd /Users/royco/ollog && grep -q "udp_transport.close()" app/main.py && echo "OK" — confirms sync close before DB shutdown.
  </verify>
  <done>Lifespan conditionally starts UDP listener when UDP_ENABLED=true, closes transport before DB on shutdown. Docker Compose exposes 2399/udp. UDP_BIND_HOST Docker note documented in compose file.</done>
</task>

</tasks>

<verification>
1. python -c "from app.config import settings; assert not settings.udp_enabled" — default is False
2. python -c "from app.udp.server import QSODatagramProtocol, start_udp_listener" — imports clean
3. python -c "from app.main import app" — full app imports without error
4. grep "2399:2399/udp" docker-compose.yml — Docker UDP port mapped
5. grep "udp_transport.close()" app/main.py — transport closed in shutdown
6. grep "UDP listener bound to" app/udp/server.py — startup banner log line exists
7. grep "UDP datagram received from" app/udp/server.py — receipt log line exists
8. grep "get_running_loop" app/udp/server.py — uses modern API, not deprecated get_event_loop
</verification>

<success_criteria>
- UDP_ENABLED=true binds socket and logs "UDP listener bound to {host}:{port}"
- UDP_ENABLED=false (default) creates no socket — app starts identically to v1.3
- Raw datagram to bound port produces log line with source address and byte count
- Transport closed synchronously before DB in shutdown sequence
- Docker Compose exposes 2399:2399/udp
- QSODatagramProtocol has _background_tasks set ready for Phase 17
- Uses asyncio.get_running_loop() throughout (Python 3.14 compatible)
</success_criteria>

<output>
After completion, create `.planning/phases/16-udp-infrastructure/16-02-SUMMARY.md`
</output>
