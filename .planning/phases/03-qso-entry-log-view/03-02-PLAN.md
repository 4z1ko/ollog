---
phase: 03-qso-entry-log-view
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - app/qso/service.py
  - app/qso/router.py
  - tests/test_duplicate_detection.py
autonomous: true

must_haves:
  truths:
    - "When POSTing a QSO that matches same CALL, BAND, MODE within +/-2 minutes of an existing QSO, the API returns 409 with duplicate info"
    - "When POSTing with force=true query param, the duplicate check is skipped and the QSO is saved"
    - "Duplicate detection only matches non-deleted QSOs — soft-deleted QSOs do not trigger warnings"
    - "Duplicate detection is scoped to the current operator — another operator's QSO with same fields does not trigger warning"
    - "The duplicate check uses qso_date_utc datetime comparison with timedelta(minutes=2)"
  artifacts:
    - path: "app/qso/service.py"
      provides: "find_duplicate function added to existing service"
      contains: "find_duplicate"
      min_lines: 60
    - path: "tests/test_duplicate_detection.py"
      provides: "Duplicate detection tests"
      contains: "test_"
      min_lines: 60
  key_links:
    - from: "app/qso/router.py"
      to: "app/qso/service.py"
      via: "find_duplicate called before insert in POST endpoint"
      pattern: "find_duplicate"
---

<objective>
Add duplicate detection to the QSO creation flow: before inserting a new QSO, check for an existing non-deleted QSO with the same CALL, BAND, MODE within a +/-2 minute window. Return 409 with duplicate info unless force=true is set.

Purpose: Operators need to be warned about potential duplicate QSOs before saving (QSO-05). This prevents accidental double-logging during contests and casual operation.
Output: find_duplicate service function, POST endpoint wired to check duplicates, and test coverage.
</objective>

<execution_context>
@/Users/royco/.claude/get-shit-done/workflows/execute-plan.md
@/Users/royco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-qso-entry-log-view/03-RESEARCH.md
@.planning/phases/03-qso-entry-log-view/03-01-SUMMARY.md
@app/qso/models.py
@app/qso/service.py
@app/qso/router.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Duplicate detection service function and POST endpoint integration</name>
  <files>app/qso/service.py, app/qso/router.py</files>
  <action>
Add to app/qso/service.py:

```python
async def find_duplicate(
    operator: str,
    call: str,
    band: str,
    mode: str,
    qso_date_utc: datetime,
) -> QSO | None:
    """Find an existing non-deleted QSO matching CALL, BAND, MODE within +/-2 min.

    Returns the duplicate QSO if found, None otherwise.
    Only checks within the same operator's QSOs (operator isolation).
    """
    window_start = qso_date_utc - timedelta(minutes=2)
    window_end = qso_date_utc + timedelta(minutes=2)

    return await QSO.find_one({
        "_operator": operator,
        "CALL": call,
        "BAND": band,
        "MODE": mode,
        "_deleted": False,
        "qso_date_utc": {"$gte": window_start, "$lte": window_end},
    })
```

Update the POST endpoint in app/qso/router.py:
- The `force: bool = Query(False)` param should already exist from 03-01 (it was declared but ignored).
- Before insert, if `force` is False:
  - Parse qso_date_utc from the built QSO dict.
  - Call `find_duplicate(operator, call=body.CALL.upper(), band=band_upper, mode=mode_upper, qso_date_utc=parsed_dt)`.
  - NOTE: Use the uppercased BAND and MODE values that will be stored, not the raw input.
  - If a duplicate is found, return HTTP 409 with body: `{"duplicate": true, "existing_id": str(dup.id), "existing_call": dup.CALL, "existing_band": dup.BAND, "existing_mode": dup.MODE, "existing_date": dup.qso_date_utc.isoformat() if dup.qso_date_utc else None}`.
- If `force` is True, skip the duplicate check entirely and proceed with insert.

CRITICAL:
- Use MongoDB field names in the query: "_operator", "_deleted" (not Python attribute names).
- Ensure qso_date_utc in the query is timezone-aware (UTC) to avoid naive/aware comparison issues.
- The duplicate check compares uppercased BAND and MODE (matching the stored values).
  </action>
  <verify>python -c "from app.qso.service import find_duplicate; print('find_duplicate OK')"</verify>
  <done>find_duplicate function queries MongoDB for matching CALL/BAND/MODE within +/-2 min window. POST endpoint returns 409 with duplicate info when match found, unless force=true.</done>
</task>

<task type="auto">
  <name>Task 2: Duplicate detection tests</name>
  <files>tests/test_duplicate_detection.py</files>
  <action>
Create tests/test_duplicate_detection.py. Do NOT modify conftest.py — declare all fixtures locally.

Local fixtures (same pattern as test_qso_api.py):
- qso_db, operator_user, op1_token, op2_user, op2_token (copy pattern from test_qso_api.py).

Helper: `create_qso(client, token, **overrides)` — POST a QSO with default values, overriding as needed.

Tests:

**Duplicate detected:**
- test_duplicate_within_2min_window: Create QSO at TIME_ON="1430". POST another with same CALL, BAND, MODE at TIME_ON="1431". Assert 409 with duplicate=true and existing_id.
- test_duplicate_exact_same_time: Create QSO. POST identical QSO. Assert 409.
- test_duplicate_at_boundary_2min: Create QSO at TIME_ON="1430". POST at TIME_ON="1432" (exactly 2 min). Assert 409 (boundary inclusive with $lte).

**No duplicate:**
- test_no_duplicate_outside_window: Create QSO at TIME_ON="1430". POST at TIME_ON="1433" (3 min later). Assert 201 (no duplicate).
- test_no_duplicate_different_call: Create QSO with CALL="W1AW". POST with CALL="K1ABC" same time/band/mode. Assert 201.
- test_no_duplicate_different_band: Same CALL/MODE/time but different BAND. Assert 201.
- test_no_duplicate_different_mode: Same CALL/BAND/time but different MODE. Assert 201.
- test_no_duplicate_deleted_qso: Create QSO, soft-delete it. POST identical QSO. Assert 201 (deleted QSOs don't count).
- test_no_duplicate_other_operator: Create QSO as op1. POST identical QSO as op2. Assert 201 (operator isolation).

**Force override:**
- test_force_skips_duplicate_check: Create QSO. POST identical QSO with ?force=true. Assert 201 (saved despite duplicate).

All tests use httpx.AsyncClient with ASGITransport(app=app).
  </action>
  <verify>docker compose up -d mongodb && pytest tests/test_duplicate_detection.py -v && docker compose down</verify>
  <done>All duplicate detection tests pass. Duplicate warning returned for matching CALL/BAND/MODE within +/-2 min. No false positives for different fields, deleted QSOs, or other operators. Force flag bypasses check.</done>
</task>

</tasks>

<verification>
- POST identical QSO within 2 min window returns 409 with duplicate info
- POST with ?force=true skips duplicate check and returns 201
- Duplicate check uses uppercased BAND/MODE
- Soft-deleted QSOs do not trigger duplicate warning
- Other operator's QSOs do not trigger duplicate warning
- Boundary at exactly 2 min is inclusive
- 3+ min apart does not trigger duplicate
- pytest tests/test_duplicate_detection.py -v passes
</verification>

<success_criteria>
Duplicate detection warns operators when logging a QSO matching same CALL, BAND, MODE within +/-2 minute window. Force flag allows saving anyway. Check is operator-scoped and excludes soft-deleted QSOs. All tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/03-qso-entry-log-view/03-02-SUMMARY.md`
</output>
