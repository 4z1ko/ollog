---
phase: 56-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/operator-guide/profile.md
  - docs/admin-guide/account-management.md
  - mkdocs.yml
autonomous: true
requirements:
  - DOC-01
  - DOC-02
user_setup: []
must_haves:
  truths:
    - "Operator-guide profile page documents the Danger Zone clear-log flow with password confirmation and a permanence warning rendered as a Material admonition"
    - "Admin-guide account-management page documents the Clear Operator Log per-operator action with admin-password confirmation and a permanence warning rendered as a Material admonition"
    - "mkdocs.yml enables the admonition markdown extension so `!!! danger` blocks render as styled callouts (not literal text)"
  artifacts:
    - path: "docs/operator-guide/profile.md"
      provides: "Danger Zone section (DOC-01)"
      contains: "## Danger Zone"
    - path: "docs/admin-guide/account-management.md"
      provides: "Clear Operator Log section (DOC-02)"
      contains: "## Clear Operator Log"
    - path: "mkdocs.yml"
      provides: "admonition markdown extension"
      contains: "markdown_extensions"
  key_links:
    - from: "docs/operator-guide/profile.md"
      to: "mkdocs admonition extension"
      via: "!!! danger \"This cannot be undone\""
      pattern: "^!!! danger"
    - from: "docs/admin-guide/account-management.md"
      to: "mkdocs admonition extension"
      via: "!!! danger \"This cannot be undone\""
      pattern: "^!!! danger"
    - from: "mkdocs.yml"
      to: "Python-Markdown admonition extension"
      via: "markdown_extensions list entry"
      pattern: "markdown_extensions:\\s*\\n\\s*-\\s*admonition"
---

<objective>
Author the two clear-log documentation sections and enable the MkDocs Material `admonition`
markdown extension so the `!!! danger` permanence warnings render as styled callouts.

Purpose: DOC-01 and DOC-02 require new doc sections. D-03 mandates the danger admonition
style. RESEARCH.md proves that without `markdown_extensions: [admonition]` in mkdocs.yml,
the `!!!` syntax silently renders as plain text — so the config change is a hard
prerequisite for the content to render correctly.

Output: Three modified files (two docs + mkdocs.yml) committed and ready for the build
step in Plan 02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-documentation/56-CONTEXT.md
@.planning/phases/56-documentation/56-RESEARCH.md
@.planning/phases/56-documentation/56-PATTERNS.md

@docs/operator-guide/profile.md
@docs/admin-guide/account-management.md
@docs/getting-started/first-qso.md
@mkdocs.yml
@templates/log/clear_log_modal.html
@templates/admin/clear_log_modal.html

<interfaces>
<!-- UI labels that the docs MUST match (extracted from the templates the docs describe) -->

From templates/log/clear_log_modal.html (operator modal):
- Trigger button label on profile page: "Clear my log"
- Modal title: "Clear my log"
- Count display: "{{ count }} QSO(s)"
- Password field label: "Your password"
- Confirm submit button text: "Delete N QSOs" (where N is the count)
- Cancel button text: "Keep my log"

From templates/admin/clear_log_modal.html (admin modal):
- Trigger button label on operators page: "Clear log"
- Modal title: "Clear {{ callsign }}'s Log"
- Subtitle: "for {{ callsign }}"
- Password field label: "Your admin password"
- Confirm submit button text: "Delete N QSOs"
- Cancel button text: "Keep log"

From templates/log/profile.html line 224–244:
- Section heading on profile page: "Danger Zone" (the docs use this exact label)

From templates/admin/users_table.html lines 80–93:
- "Clear log" button is in the per-operator row actions column alongside enable/disable/reset-password

From mkdocs.yml current state:
- File has NO `markdown_extensions` block at all
- `not_in_nav: | openapi.json` MUST NOT be removed or changed (--strict will fail)
- Insertion point: after the `plugins:` block (ends at line 20), before `nav:` (line 22)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable admonition extension in mkdocs.yml</name>
  <files>mkdocs.yml</files>
  <read_first>
    - mkdocs.yml (current state — has no markdown_extensions block; insertion goes between `plugins:` and `nav:`)
    - .planning/phases/56-documentation/56-RESEARCH.md (Pattern 1, Pitfall 1)
    - .planning/phases/56-documentation/56-PATTERNS.md (`mkdocs.yml` section, Full target placement)
  </read_first>
  <action>
    Insert a new top-level `markdown_extensions` YAML block into `mkdocs.yml` between the
    existing `plugins:` block and the `nav:` block. The exact block to insert:

    ```yaml

    markdown_extensions:
      - admonition
    ```

    Placement: immediately after the line `      syntaxHighlightTheme: monokai` (current
    line 20, end of the `swagger-ui-tag` plugin entry) and before `nav:` (current line 22).
    Preserve a single blank line on each side of the new block (matches the spacing
    between `plugins:` and `nav:` today).

    DO NOT modify, remove, or reorder:
    - The `not_in_nav: | openapi.json` block (lines 5–6) — load-bearing for `--strict`
    - The existing `plugins:` block content
    - The existing `theme:` block
    - The `nav:` block
    - `site_name`, `site_url`, `use_directory_urls`

    Why: per RESEARCH.md Pitfall 1, without this extension the `!!! danger` syntax in
    Tasks 2 and 3 renders as literal `!!! danger ...` text in the output HTML with no build
    warning. This is the ONLY blocking prerequisite for the new doc sections.
  </action>
  <verify>
    <automated>grep -nE "^markdown_extensions:" /Users/royco/ollog/mkdocs.yml &amp;&amp; grep -nE "^\s+- admonition$" /Users/royco/ollog/mkdocs.yml &amp;&amp; grep -nE "^not_in_nav:" /Users/royco/ollog/mkdocs.yml &amp;&amp; uv run python -c "import yaml; yaml.safe_load(open('/Users/royco/ollog/mkdocs.yml'))"</automated>
  </verify>
  <done>
    `mkdocs.yml` parses as valid YAML, contains a top-level `markdown_extensions:` key
    with `- admonition` as a list item, AND retains the original `not_in_nav:` block.
  </done>
  <acceptance_criteria>
    - `grep -c "^markdown_extensions:" mkdocs.yml` returns 1
    - `grep -c "^  - admonition$" mkdocs.yml` returns 1
    - `grep -c "^not_in_nav:" mkdocs.yml` returns 1 (unchanged)
    - `grep -c "^plugins:" mkdocs.yml` returns 1 (unchanged)
    - `grep -c "swagger-ui-tag:" mkdocs.yml` returns 1 (unchanged)
    - `python -c "import yaml; d=yaml.safe_load(open('mkdocs.yml')); assert 'admonition' in d['markdown_extensions']"` exits 0
    - `markdown_extensions` appears AFTER `plugins:` and BEFORE `nav:` in the file
      (verify: `awk '/^plugins:/{p=NR} /^markdown_extensions:/{m=NR} /^nav:/{n=NR} END{exit !(p<m && m<n)}' mkdocs.yml`)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Append Danger Zone section to operator profile docs (DOC-01)</name>
  <files>docs/operator-guide/profile.md</files>
  <read_first>
    - docs/operator-guide/profile.md (current state — section will append at end of file, after the existing `## STATION_CALLSIGN Environment Variable` section ending at line 86)
    - docs/getting-started/first-qso.md (numbered-step style reference, lines 33–42)
    - templates/log/clear_log_modal.html (operator modal — confirms exact UI labels)
    - templates/log/profile.html (confirms "Danger Zone" heading and "Clear my log" button label)
    - .planning/phases/56-documentation/56-PATTERNS.md (operator section pattern, exact prose to append)
    - .planning/phases/56-documentation/56-CONTEXT.md (D-01, D-03)
  </read_first>
  <action>
    Append the following section verbatim to the end of `docs/operator-guide/profile.md`,
    after line 86 (the `## STATION_CALLSIGN Environment Variable` section). Add ONE blank
    line between the current end-of-file content and the new heading.

    Exact content to append (keep the indentation and blank lines exactly as shown — the
    admonition body is indented by 4 spaces, which is required by Python-Markdown):

    ```markdown

    ## Danger Zone

    The **Danger Zone** section appears at the bottom of your profile page and contains
    irreversible account actions.

    To clear your log:

    1. Navigate to **Profile** in the navigation bar.
    2. Scroll to the **Danger Zone** section at the bottom of the page.
    3. Click **Clear my log**.
    4. A confirmation modal opens showing the number of QSOs that will be deleted.
    5. Enter your password in the **Your password** field.
    6. Click **Delete N QSOs** (where N is the count shown) to confirm, or **Keep my log**
       to cancel.

    !!! danger "This cannot be undone"
        Clearing your log permanently deletes all your QSOs from the database. There is no
        undo and no recovery from the UI. If you need to recover deleted QSOs, restore from
        a backup taken before the clear operation.
    ```

    Notes on the prose (locked):
    - Section heading must be exactly `## Danger Zone` (matches the UI section heading on
      `templates/log/profile.html` and the D-01 wording).
    - UI labels in **bold** must match the modal verbatim: `Clear my log`, `Your password`,
      `Delete N QSOs`, `Keep my log`, `Profile`, `Danger Zone`.
    - Admonition uses exact title `"This cannot be undone"` (D-03).
    - Body of admonition mentions all three required points: permanent deletion, no UI
      recovery, backup is the only recovery path (CONTEXT.md Claude's Discretion).

    DO NOT modify any existing content above line 86. Only append.
  </action>
  <verify>
    <automated>grep -c "^## Danger Zone$" /Users/royco/ollog/docs/operator-guide/profile.md &amp;&amp; grep -c '^!!! danger "This cannot be undone"$' /Users/royco/ollog/docs/operator-guide/profile.md &amp;&amp; grep -c "Clear my log" /Users/royco/ollog/docs/operator-guide/profile.md &amp;&amp; grep -c "STATION_CALLSIGN Environment Variable" /Users/royco/ollog/docs/operator-guide/profile.md</automated>
  </verify>
  <done>
    The file contains the new `## Danger Zone` section appended at the end with the
    numbered steps and the danger admonition; all pre-existing sections are preserved.
  </done>
  <acceptance_criteria>
    - `grep -c "^## Danger Zone$" docs/operator-guide/profile.md` returns 1
    - `grep -c '^!!! danger "This cannot be undone"$' docs/operator-guide/profile.md` returns 1
    - `grep -c "\*\*Clear my log\*\*" docs/operator-guide/profile.md` returns at least 1
    - `grep -c "\*\*Your password\*\*" docs/operator-guide/profile.md` returns 1
    - `grep -c "\*\*Delete N QSOs\*\*" docs/operator-guide/profile.md` returns 1
    - `grep -c "\*\*Keep my log\*\*" docs/operator-guide/profile.md` returns 1
    - `grep -c "permanently deletes" docs/operator-guide/profile.md` returns at least 1
    - `grep -c "restore from" docs/operator-guide/profile.md` returns at least 1
    - The string `## STATION_CALLSIGN Environment Variable` still appears (existing section preserved)
    - Line containing `## Danger Zone` has line number greater than line containing
      `## STATION_CALLSIGN Environment Variable` (verify: `awk '/^## STATION_CALLSIGN/{s=NR} /^## Danger Zone/{d=NR} END{exit !(s<d)}' docs/operator-guide/profile.md`)
    - The four lines following `!!! danger` are indented by exactly 4 spaces (verify:
      `awk '/^!!! danger/{f=1; next} f && /^[^ ]/ && !/^$/{exit 1} f && /^    [^ ]/{c++} END{exit !(c>=1)}' docs/operator-guide/profile.md`)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Append Clear Operator Log section to admin docs (DOC-02)</name>
  <files>docs/admin-guide/account-management.md</files>
  <read_first>
    - docs/admin-guide/account-management.md (current state — `## Reset a User's Password` section ends at line 78; new section appends after that)
    - templates/admin/clear_log_modal.html (admin modal — confirms exact UI labels including admin-specific password label)
    - templates/admin/users_table.html (confirms "Clear log" button placement in per-operator row)
    - .planning/phases/56-documentation/56-PATTERNS.md (admin section pattern, exact prose to append)
    - .planning/phases/56-documentation/56-CONTEXT.md (D-02, D-03)
  </read_first>
  <action>
    Append the following section verbatim to the end of
    `docs/admin-guide/account-management.md`, after line 78 (the closing paragraph of the
    `## Reset a User's Password` section ending with "...does not immediately revoke active
    sessions."). Add ONE blank line between the existing last line and the new heading.

    Exact content to append (note: NO curl block — this feature is UI-only, no REST endpoint):

    ```markdown

    ## Clear Operator Log

    Admins can permanently delete all QSOs for any operator from the operators management
    page. This action is available in the admin web UI only — there is no REST API
    endpoint for it.

    1. In the admin web UI, navigate to the **Operators** management page.
    2. Find the operator whose log you want to clear.
    3. Click the **Clear log** button in that operator's row, alongside the existing
       enable/disable and reset-password actions.
    4. A confirmation modal opens showing the operator's callsign and the number of QSOs
       that will be deleted.
    5. Enter your admin password in the **Your admin password** field.
    6. Click **Delete N QSOs** (where N is the count shown) to confirm, or **Keep log**
       to cancel.

    !!! danger "This cannot be undone"
        Clearing an operator's log permanently deletes all their QSOs from the database.
        There is no undo and no recovery from the UI. If you need to recover deleted QSOs,
        restore from a backup taken before the clear operation.
    ```

    Notes on the prose (locked):
    - Section heading must be exactly `## Clear Operator Log` (D-02).
    - Section MUST be placed AFTER `## Reset a User's Password` (D-02 specifies action
      grouping consistent with how the actions appear on the admin operators page).
    - UI labels in **bold** must match the admin modal verbatim: `Operators`,
      `Clear log`, `Your admin password` (note: "admin" prefix distinguishes from operator
      modal), `Delete N QSOs`, `Keep log` (no "my").
    - Admonition uses exact title `"This cannot be undone"` (D-03), same wording style as
      DOC-01 but body references "an operator's log" instead of "your log".
    - The "no REST API endpoint" sentence is intentional — RESEARCH.md notes this section
      has no curl block (existing Reset Password section has one because that endpoint
      exists; Clear Operator Log does not).

    DO NOT modify any existing content above line 78. Only append.
  </action>
  <verify>
    <automated>grep -c "^## Clear Operator Log$" /Users/royco/ollog/docs/admin-guide/account-management.md &amp;&amp; grep -c '^!!! danger "This cannot be undone"$' /Users/royco/ollog/docs/admin-guide/account-management.md &amp;&amp; grep -c "Reset a User's Password" /Users/royco/ollog/docs/admin-guide/account-management.md &amp;&amp; awk '/^## Reset a User/{r=NR} /^## Clear Operator Log/{c=NR} END{exit !(r&gt;0 &amp;&amp; c&gt;0 &amp;&amp; r&lt;c)}' /Users/royco/ollog/docs/admin-guide/account-management.md</automated>
  </verify>
  <done>
    The file contains the new `## Clear Operator Log` section appended after the existing
    Reset Password section, with the numbered steps and danger admonition; all pre-existing
    sections are preserved.
  </done>
  <acceptance_criteria>
    - `grep -c "^## Clear Operator Log$" docs/admin-guide/account-management.md` returns 1
    - `grep -c '^!!! danger "This cannot be undone"$' docs/admin-guide/account-management.md` returns 1
    - `grep -c "\*\*Operators\*\*" docs/admin-guide/account-management.md` returns at least 1
    - `grep -c "\*\*Clear log\*\*" docs/admin-guide/account-management.md` returns 1
    - `grep -c "\*\*Your admin password\*\*" docs/admin-guide/account-management.md` returns 1
    - `grep -c "\*\*Delete N QSOs\*\*" docs/admin-guide/account-management.md` returns 1
    - `grep -c "\*\*Keep log\*\*" docs/admin-guide/account-management.md` returns 1
    - `grep -c "no REST API endpoint" docs/admin-guide/account-management.md` returns 1
    - `grep -c "permanently deletes" docs/admin-guide/account-management.md` returns at least 1
    - The string `## Reset a User's Password` still appears (existing section preserved)
    - `## Clear Operator Log` appears AFTER `## Reset a User's Password` (verify with awk
      ordering check shown in the verify command above)
    - The lines following `!!! danger` are indented by exactly 4 spaces (verify:
      `awk '/^!!! danger/{f=1; next} f && /^[^ ]/ && !/^$/{exit 1} f && /^    [^ ]/{c++} END{exit !(c>=1)}' docs/admin-guide/account-management.md`)
    - The new section contains NO ` ```bash ` code block (confirms UI-only documentation
      style: `awk '/^## Clear Operator Log/{f=1} f && /^## /{if(NR&gt;1 &amp;&amp; !/Clear Operator Log/) f=0} f && /^```bash/{exit 1} END{exit 0}' docs/admin-guide/account-management.md`)
  </acceptance_criteria>
</task>

</tasks>

<verification>
After all three tasks complete, run a sanity check that the markdown is parseable and the
admonition prerequisite is in place by doing a dry-run build (the full build with `--strict`
is the responsibility of Plan 02):

```bash
# Confirm YAML is valid and admonition is enabled
uv run python -c "import yaml; d=yaml.safe_load(open('mkdocs.yml')); assert 'admonition' in d.get('markdown_extensions', []), 'admonition not enabled'"

# Confirm both new sections exist
grep -q "^## Danger Zone$" docs/operator-guide/profile.md
grep -q "^## Clear Operator Log$" docs/admin-guide/account-management.md
```
</verification>

<success_criteria>
- `mkdocs.yml` contains `markdown_extensions: [admonition]` block placed after `plugins:` and before `nav:`
- `not_in_nav: | openapi.json` block in `mkdocs.yml` is unchanged
- `docs/operator-guide/profile.md` has `## Danger Zone` section appended with numbered steps and `!!! danger "This cannot be undone"` admonition
- `docs/admin-guide/account-management.md` has `## Clear Operator Log` section appended AFTER `## Reset a User's Password` with numbered steps and `!!! danger "This cannot be undone"` admonition
- Both new sections explicitly state deletion is permanent and recovery requires a backup
- All UI labels in the docs match the modal templates verbatim (Clear my log / Clear log, Your password / Your admin password, Delete N QSOs, Keep my log / Keep log)
- No existing doc content is modified — both edits are pure appends
</success_criteria>

<output>
After completion, create `.planning/phases/56-documentation/56-01-SUMMARY.md` describing:
- Files modified and the line range of new content in each
- Confirmation that mkdocs.yml admonition extension is in place (DOC-03 prerequisite)
- Any prose deviations from the PATTERNS.md illustrative content (Claude's discretion areas)
- Confirmation that `## Reset a User's Password` ordering is preserved
</output>
