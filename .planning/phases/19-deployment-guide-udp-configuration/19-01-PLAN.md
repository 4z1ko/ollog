---
phase: 19-deployment-guide-udp-configuration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/deployment.md
autonomous: true
must_haves:
  truths:
    - "The Environment Variables table in deployment.md includes UDP_ENABLED, UDP_PORT, UDP_BIND_HOST, and UDP_OPERATOR rows"
    - "Each UDP env var row has correct type, default, and description matching app/config.py"
    - "A new section 'Enabling the UDP Listener' exists after the Bootstrap Admin Account section"
    - "The section contains a Docker Compose snippet with the 2399:2399/udp port mapping and UDP_ENABLED, UDP_BIND_HOST, UDP_OPERATOR env vars"
    - "Port 2399 is used consistently — 2237 does not appear anywhere"
    - "UDP_BIND_HOST description explicitly states Docker requires 0.0.0.0"
    - "UDP_OPERATOR is noted as required when UDP_ENABLED=true"
  artifacts:
    - path: "docs/deployment.md"
      provides: "UDP configuration documentation for operators"
      contains: "UDP_ENABLED"
  key_links:
    - from: "docs/deployment.md env var table"
      to: "app/config.py UDP settings"
      via: "env var names, types, defaults"
      pattern: "UDP_ENABLED.*false.*UDP_PORT.*2399.*UDP_BIND_HOST.*127\\.0\\.0\\.1.*UDP_OPERATOR"
    - from: "docs/deployment.md compose snippet"
      to: "docker-compose.yml api service"
      via: "port mapping and env var values"
      pattern: "2399:2399/udp"
---

<objective>
Update docs/deployment.md with UDP configuration documentation.

Purpose: Operators who want to enable UDP ADIF reception need to know which env vars to set and how to configure their Docker Compose setup. Without this documentation they must read source code to discover the UDP feature.
Output: Updated docs/deployment.md with four new env var rows and a new "Enabling the UDP Listener" section.
</objective>

<execution_context>
@/Users/royco/.claude/get-shit-done/workflows/execute-plan.md
@/Users/royco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/royco/ollog/.planning/PROJECT.md
@/Users/royco/ollog/.planning/ROADMAP.md
@/Users/royco/ollog/.planning/STATE.md
@/Users/royco/ollog/.planning/phases/19-deployment-guide-udp-configuration/19-RESEARCH.md
@/Users/royco/ollog/docs/deployment.md
@/Users/royco/ollog/docker-compose.yml
@/Users/royco/ollog/app/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add UDP env vars to the Environment Variables table</name>
  <files>/Users/royco/ollog/docs/deployment.md</files>
  <action>
    In `docs/deployment.md`, append four rows to the Environment Variables table immediately after the `ADMIN_CALLSIGN` row (currently line 55). The table uses the column order `Variable | Required | Default | Description`. Add these exact rows:

    ```markdown
    | UDP_ENABLED | No | `false` | Set to `true` to start the UDP ADIF listener. |
    | UDP_PORT | No | `2399` | UDP port the listener binds to. Must match the Docker port mapping if changed. |
    | UDP_BIND_HOST | No | `127.0.0.1` | Address the UDP socket binds to. Inside Docker, set to `0.0.0.0` so host traffic reaches the container. |
    | UDP_OPERATOR | No | (none) | Operator callsign assigned to QSOs received via UDP. Required when `UDP_ENABLED=true`. |
    ```

    Do not change any existing rows. Do not change column headers or table formatting style.
  </action>
  <verify>
    Run: `grep -c "UDP_" docs/deployment.md`
    Expected: 4 (one match per UDP variable name — UDP_ENABLED, UDP_PORT, UDP_BIND_HOST, UDP_OPERATOR).

    Also run: `grep "2237" docs/deployment.md`
    Expected: no output (port 2237 must not appear).
  </verify>
  <done>
    The Environment Variables table contains exactly four new UDP rows with defaults matching app/config.py (false, 2399, 127.0.0.1, none). No mention of port 2237 anywhere in the file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add "Enabling the UDP Listener" section</name>
  <files>/Users/royco/ollog/docs/deployment.md</files>
  <action>
    In `docs/deployment.md`, insert a new `## Enabling the UDP Listener` section after the `## Bootstrap Admin Account` section (after the paragraph ending "The account persists in the database." around line 71) and before `## Verification Steps`.

    The section content:

    ```markdown
    ## Enabling the UDP Listener

    ollog ships with a UDP ADIF listener that accepts datagrams from logging software such as WSJT-X, N1MM+, and Log4OM. The listener is disabled by default and is enabled via environment variables.

    To enable UDP in Docker Compose, add the following to the `api` service in your `docker-compose.yml` (the port mapping is already present in the default file):

    ```yaml
    services:
      api:
        ports:
          - "2399:2399/udp"   # UDP ADIF listener port
        environment:
          - UDP_ENABLED=true
          - UDP_BIND_HOST=0.0.0.0   # required inside Docker
          - UDP_OPERATOR=N0CALL     # replace with the operator's callsign
    ```

    `UDP_OPERATOR` must be set to a callsign that has an existing operator account in ollog. QSOs received via UDP are logged under that callsign. If the callsign is not found, the listener logs a WARNING and drops the datagram.

    If you change `UDP_PORT` from the default `2399`, update the Docker port mapping to match (e.g., `2400:2400/udp`).
    ```

    Use a fenced code block with the `yaml` language tag for the snippet. Preserve the surrounding section headings (Bootstrap Admin Account above, Verification Steps below).
  </action>
  <verify>
    Run: `grep -n "Enabling the UDP Listener" docs/deployment.md`
    Expected: one line showing the section heading.

    Run: `grep "2399:2399/udp" docs/deployment.md`
    Expected: one line (inside the compose snippet).

    Run: `grep -c "## " docs/deployment.md`
    Expected: one more `##` heading than before (7 previously, now 8).
  </verify>
  <done>
    The "Enabling the UDP Listener" section exists between "Bootstrap Admin Account" and "Verification Steps". The section contains a Docker Compose snippet showing 2399:2399/udp port mapping, UDP_ENABLED=true, UDP_BIND_HOST=0.0.0.0, and UDP_OPERATOR. A note explains UDP_OPERATOR must be an existing account.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `grep -n "UDP_" docs/deployment.md` — should show 4+ lines covering all four UDP env vars in the table and in the section body
2. `grep "2237" docs/deployment.md` — should return nothing
3. `grep "2399" docs/deployment.md` — should return lines in both the env var table (UDP_PORT default) and the compose snippet
4. Read the file top-to-bottom and confirm section order: Quick Start → Environment Variables → Bootstrap Admin Account → Enabling the UDP Listener → Verification Steps → Updating → MongoDB Replica Set
</verification>

<success_criteria>
- docs/deployment.md contains four UDP env var rows (UDP_ENABLED, UDP_PORT, UDP_BIND_HOST, UDP_OPERATOR) in the Environment Variables table with correct defaults (false, 2399, 127.0.0.1, none)
- docs/deployment.md contains a "Enabling the UDP Listener" section with a Docker Compose snippet showing port 2399 and the three env vars to set
- UDP_BIND_HOST description calls out the Docker 0.0.0.0 requirement
- UDP_OPERATOR is documented as required when UDP_ENABLED=true
- Port 2237 does not appear anywhere in the file
</success_criteria>

<output>
After completion, create `/Users/royco/ollog/.planning/phases/19-deployment-guide-udp-configuration/19-01-SUMMARY.md` using the summary template.
</output>
