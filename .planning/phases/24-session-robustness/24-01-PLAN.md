---
phase: 24-session-robustness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/config.py
  - .env
  - docs/deployment.md
autonomous: true

must_haves:
  truths:
    - "JWT_EXPIRE_MINUTES env var controls session lifetime (already true, unchanged)"
    - "Default session lifetime is 480 minutes, sufficient for an 8-hour FT8 session"
    - "Documentation reflects the new default value"
  artifacts:
    - path: "app/config.py"
      provides: "Code default for jwt_expire_minutes"
      contains: "jwt_expire_minutes: int = 480"
    - path: ".env"
      provides: "Operator local config"
      contains: "JWT_EXPIRE_MINUTES=480"
    - path: "docs/deployment.md"
      provides: "Documented default value"
      contains: "`480`"
  key_links:
    - from: "app/config.py"
      to: "app/auth/service.py"
      via: "settings.jwt_expire_minutes already read at token creation"
      pattern: "settings\\.jwt_expire_minutes"
---

<objective>
Raise the default JWT session lifetime from 60 minutes to 480 minutes so operators running overnight FT8 logging sessions are not silently logged out mid-session.

Purpose: An 8-hour FT8 session currently expires after 1 hour, breaking the live log table feature. Raising the default to 480 minutes covers a full session.
Output: Updated default in code, .env, and docs.
</objective>

<execution_context>
@/Users/royco/.claude/get-shit-done/workflows/execute-plan.md
@/Users/royco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@app/config.py
@app/auth/service.py
@docs/deployment.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Raise JWT expiry default to 480 minutes</name>
  <files>app/config.py, .env, docs/deployment.md</files>
  <action>
1. In `app/config.py` line 9, change `jwt_expire_minutes: int = 60` to `jwt_expire_minutes: int = 480`.
2. In `.env` line 16, change `JWT_EXPIRE_MINUTES=60` to `JWT_EXPIRE_MINUTES=480`.
3. In `docs/deployment.md` line 52, in the environment variables table, change the Default column for `JWT_EXPIRE_MINUTES` from `` `60` `` to `` `480` `` and update the Description from "Token lifetime in minutes" to "Token lifetime in minutes (default covers an 8-hour session)".

No other files need changes. `app/auth/service.py` already reads `settings.jwt_expire_minutes` at token creation time — no modification required there.
  </action>
  <verify>
1. `grep 'jwt_expire_minutes: int = 480' app/config.py` — must match exactly one line
2. `grep 'JWT_EXPIRE_MINUTES=480' .env` — must match exactly one line
3. `grep '480' docs/deployment.md` — must appear in the JWT_EXPIRE_MINUTES row
4. `cd /Users/royco/ollog && python -c "from app.config import Settings; s = Settings(_env_file=''); assert s.jwt_expire_minutes == 480, f'got {s.jwt_expire_minutes}'"` — confirms code default is 480 without reading .env
  </verify>
  <done>Code default is 480, .env is 480, docs show 480. No behavioral change other than longer session lifetime.</done>
</task>

</tasks>

<verification>
- `grep -n 'jwt_expire_minutes' app/config.py` shows `int = 480`
- `grep -n 'JWT_EXPIRE_MINUTES' .env` shows `=480`
- `grep -n 'JWT_EXPIRE_MINUTES' docs/deployment.md` shows `480` in the default column
- Python import test confirms Settings().jwt_expire_minutes defaults to 480
</verification>

<success_criteria>
1. The JWT_EXPIRE_MINUTES environment variable controls session lifetime (was already true, still true).
2. With default configuration, session lifetime is 480 minutes — sufficient for an 8-hour FT8 session.
3. Documentation accurately reflects the new default.
</success_criteria>

<output>
After completion, create `.planning/phases/24-session-robustness/24-01-SUMMARY.md`
</output>
