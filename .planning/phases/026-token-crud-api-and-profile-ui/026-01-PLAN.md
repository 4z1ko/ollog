---
phase: 026-token-crud-api-and-profile-ui
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/tokens/models.py
  - app/tokens/service.py
  - app/tokens/router.py
  - app/qso/ui_router.py
  - app/main.py
  - templates/log/profile.html
  - templates/log/tokens_list.html
  - templates/log/token_created.html
  - tests/test_token_api.py
autonomous: true

must_haves:
  truths:
    - "Operator can submit a token creation form in Profile Settings with a name and optional expiry; the response shows the full plaintext token in a banner marked 'will not be shown again'"
    - "After dismissing the creation banner, the plaintext token cannot be recovered — the profile page shows only token prefix, label, creation date, and expiry"
    - "The active token list displays label, creation date, expiry (or 'Never'), and the first 8 characters of the token for identification"
    - "Operator can revoke any individual token; a subsequent REST API request with that token returns 401 (Phase 27 will enforce this — Phase 26 must set enabled=False)"
  artifacts:
    - path: "app/tokens/models.py"
      provides: "ApiToken Beanie document with expires_at field"
      contains: "expires_at: Optional[datetime] = None"
    - path: "app/tokens/service.py"
      provides: "token_is_active() helper for Phase 27"
      exports: ["token_is_active"]
    - path: "app/tokens/router.py"
      provides: "REST CRUD: POST /api/tokens, GET /api/tokens, DELETE /api/tokens/{id}"
      exports: ["router"]
    - path: "app/qso/ui_router.py"
      provides: "HTMX UI routes: POST /log/tokens/create, GET /log/tokens, DELETE /log/tokens/{id}"
    - path: "templates/log/tokens_list.html"
      provides: "Token list partial (table rows or empty-state card)"
    - path: "templates/log/token_created.html"
      provides: "Show-once token reveal banner partial"
    - path: "templates/log/profile.html"
      provides: "Token creation form section + lazy-loaded token list section"
    - path: "tests/test_token_api.py"
      provides: "Integration tests covering create, list, revoke, auth checks"
  key_links:
    - from: "templates/log/profile.html"
      to: "/log/tokens/create"
      via: "hx-post on creation form"
      pattern: "hx-post=\"/log/tokens/create\""
    - from: "templates/log/profile.html"
      to: "/log/tokens"
      via: "hx-trigger=load on token list div"
      pattern: "hx-trigger=\"load\""
    - from: "templates/log/token_created.html"
      to: "/log/tokens"
      via: "dismiss button hx-get refreshes token list"
      pattern: "hx-get=\"/log/tokens\""
    - from: "app/tokens/router.py"
      to: "ApiToken.user_id == user.id"
      via: "user_id filter on every query"
      pattern: "ApiToken\\.user_id == user\\.id"
    - from: "app/main.py"
      to: "app/tokens/router.py"
      via: "app.include_router(token_router)"
      pattern: "include_router\\(token_router\\)"
---

<objective>
Deliver the complete Token CRUD API and Profile UI for Phase 26: REST endpoints for token
lifecycle (create/list/revoke) behind JWT Bearer auth, HTMX-driven token section in Profile
Settings with a show-once plaintext reveal banner, and an integration test suite covering
all four TOK requirements.

Purpose: Operators need to create named API tokens from the Profile Settings page so that
REST API and UDP ADIF callers can authenticate as that operator in Phases 27–28.
Output: REST router at /api/tokens, UI routes under /log/tokens, profile page token sections,
two new template partials, and passing integration tests.
</objective>

<execution_context>
@/Users/royco/.claude/get-shit-done/workflows/execute-plan.md
@/Users/royco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/royco/ollog/.planning/PROJECT.md
@/Users/royco/ollog/.planning/ROADMAP.md
@/Users/royco/ollog/.planning/STATE.md

<!-- Core source files being modified or referenced -->
@/Users/royco/ollog/app/tokens/models.py
@/Users/royco/ollog/app/tokens/service.py
@/Users/royco/ollog/app/auth/dependencies.py
@/Users/royco/ollog/app/qso/ui_router.py
@/Users/royco/ollog/app/main.py
@/Users/royco/ollog/templates/log/profile.html

<!-- Pattern references (read for conventions, do not modify) -->
@/Users/royco/ollog/templates/log/qso_row.html
@/Users/royco/ollog/app/admin/ui_router.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patch ApiToken model and add token_is_active() service helper</name>
  <files>app/tokens/models.py, app/tokens/service.py</files>
  <action>
    In app/tokens/models.py, add the missing expires_at field to ApiToken between
    last_used_at and enabled:

        expires_at: Optional[datetime] = None

    This is a non-breaking additive change — existing MongoDB documents without this
    field will deserialize with expires_at=None (Pydantic v2 default for Optional with
    None default). No migration required.

    In app/tokens/service.py, add the token_is_active() helper at the bottom of the file
    (after validate_token_name). Import datetime and timezone are already available in
    the module via app/tokens/models.py context — add them to service.py imports if not
    present:

        from datetime import datetime, timezone
        from app.tokens.models import ApiToken

        def token_is_active(token: ApiToken) -> bool:
            """Return True if token is enabled and not expired.

            Checks both enabled flag (set to False on revoke) and expires_at
            (None means never expires). Used by Phase 27 X-API-Key auth dependency.
            """
            if not token.enabled:
                return False
            if token.expires_at is not None and token.expires_at <= datetime.now(tz=timezone.utc):
                return False
            return True

    NOTE: Avoid circular imports — service.py currently has no imports from app.tokens.
    Add the ApiToken import inside the function body or use TYPE_CHECKING if needed.
    Simplest approach: inline the logic without importing ApiToken, accepting an Any type
    hint, or use a duck-typed approach. Preferred: pass token as a plain object and check
    .enabled and .expires_at attributes directly with type hint `Any` from typing.
    The function will be imported by Phase 27 which will have the full ApiToken type
    available.
  </action>
  <verify>
    python -c "from app.tokens.models import ApiToken; import inspect; src = inspect.getsource(ApiToken); assert 'expires_at' in src, 'expires_at missing'"
    python -c "from app.tokens.service import token_is_active; print('token_is_active OK')"
  </verify>
  <done>
    app/tokens/models.py has expires_at: Optional[datetime] = None on ApiToken.
    app/tokens/service.py exports token_is_active() that checks both .enabled and .expires_at.
    Both modules import cleanly with no circular import errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: REST token router and HTMX UI routes, wired into main.py</name>
  <files>app/tokens/router.py, app/qso/ui_router.py, app/main.py</files>
  <action>
    CREATE app/tokens/router.py — REST CRUD under /api/tokens, Bearer JWT auth only.

    Imports needed:
      from datetime import datetime, timezone
      from typing import Annotated, Optional
      from fastapi import APIRouter, Depends, Form, HTTPException
      from fastapi.responses import Response
      from beanie import PydanticObjectId
      from pydantic import BaseModel
      from app.auth.dependencies import get_current_user
      from app.auth.models import User
      from app.tokens.models import ApiToken
      from app.tokens.service import generate_api_token, hash_api_token, validate_token_name

    Router definition:
      router = APIRouter(prefix="/api/tokens", tags=["tokens"])

    Pydantic response schema (in the same file):
      class TokenResponse(BaseModel):
          id: str
          name: str
          token_prefix: str
          created_at: datetime
          expires_at: Optional[datetime]
          enabled: bool

      class TokenCreateResponse(TokenResponse):
          full_token: str  # present only in POST response — never again

    POST / (status_code=201):
      - Form params: name (str), expires_at (Optional[str] = None)
      - Validate name with validate_token_name() — raise HTTPException(422) on ValueError
      - Parse expires_at: if provided and non-empty, parse with
        datetime.strptime(value.strip(), "%Y-%m-%d").replace(tzinfo=timezone.utc)
        Wrap in try/except ValueError → raise HTTPException(422, "Invalid date format: use YYYY-MM-DD")
      - generate_api_token() → (full_token, token_prefix)
      - hash_api_token(full_token) → hashed_token
      - Insert ApiToken(user_id=user.id, name=name, token_prefix=token_prefix,
          hashed_token=hashed_token, expires_at=expires_at_dt)
      - Return TokenCreateResponse with full_token included

    GET / (status_code=200):
      - Query: ApiToken.find(ApiToken.user_id == user.id, ApiToken.enabled == True)
              .sort(-ApiToken.created_at).to_list()
      - Return list of TokenResponse (no full_token)

    DELETE /{token_id} (status_code=204):
      - Parse token_id with PydanticObjectId — catch Exception → HTTPException(404)
      - Fetch token: await ApiToken.get(oid)
      - Verify token is not None and token.user_id == user.id and token.enabled
        → HTTPException(404, "Token not found") if any check fails
      - await token.set({ApiToken.enabled: False})
      - Return Response(status_code=204)

    ---

    MODIFY app/qso/ui_router.py — add HTMX token UI routes at the end of the file.

    Additional imports needed (add to existing import block):
      from app.tokens.models import ApiToken
      from app.tokens.service import generate_api_token, hash_api_token, validate_token_name

    Three new routes on the existing ui_router (prefix="/log"):

    GET /tokens (response_class=HTMLResponse):
      - Depends: user: User = Depends(get_current_user_cookie)
      - Query active tokens: ApiToken.find(ApiToken.user_id == user.id,
          ApiToken.enabled == True).sort(-ApiToken.created_at).to_list()
      - Return templates.TemplateResponse(request, "log/tokens_list.html",
          {"tokens": tokens})
      - HTTP 200 always

    POST /tokens/create (response_class=HTMLResponse):
      - Depends: user: User = Depends(get_current_user_cookie)
      - Form params: name: Annotated[str, Form()],
                     expires_at: Annotated[Optional[str], Form()] = None
      - Validate name with validate_token_name() — on ValueError return 200 with
        templates.TemplateResponse(request, "log/token_created.html",
          {"error": str(exc), "full_token": None})
      - Parse expires_at if provided and non-empty:
        datetime.strptime(expires_at.strip(), "%Y-%m-%d").replace(tzinfo=timezone.utc)
        On ValueError return 200 with error template
      - generate_api_token() → (full_token, token_prefix)
      - hash_api_token(full_token) → hashed_token
      - Insert ApiToken
      - Return templates.TemplateResponse(request, "log/token_created.html",
          {"error": None, "full_token": full_token, "token_id": str(doc.id)})
      - HTTP 200 always (HTMX 2.x convention)

    DELETE /tokens/{token_id} (response_class=HTMLResponse):
      - Depends: user: User = Depends(get_current_user_cookie)
      - Parse token_id with PydanticObjectId — on exception return
        Response(content="", status_code=200)  # silent no-op for missing rows
      - Fetch token, verify user_id ownership and enabled == True
        → on failure return Response(content="", status_code=200)
      - await token.set({ApiToken.enabled: False})
      - Return Response(content="", status_code=200)
        (empty body + hx-swap="outerHTML" removes the row from DOM)

    ---

    MODIFY app/main.py — register token REST router.

    Add after the profile_router registration block (around line 141):
      # Token API router
      from app.tokens.router import router as token_router  # noqa: E402
      app.include_router(token_router)
  </action>
  <verify>
    cd /Users/royco/ollog && python -c "from app.tokens.router import router; print('REST router OK')"
    cd /Users/royco/ollog && python -c "from app.qso.ui_router import ui_router; print('ui_router OK')"
    cd /Users/royco/ollog && python -c "from app.main import app; routes = [r.path for r in app.routes]; assert any('/api/tokens' in p for p in routes), 'token router not registered'"
  </verify>
  <done>
    app/tokens/router.py exists with POST /api/tokens (201), GET /api/tokens (200),
    DELETE /api/tokens/{token_id} (204) — all require Bearer JWT.
    app/qso/ui_router.py has GET /log/tokens, POST /log/tokens/create,
    DELETE /log/tokens/{token_id} — all use cookie auth, return 200 always.
    app/main.py registers token_router. All three modules import cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 3: Templates, profile page token sections, and integration tests</name>
  <files>
    templates/log/token_created.html,
    templates/log/tokens_list.html,
    templates/log/profile.html,
    tests/test_token_api.py
  </files>
  <action>
    CREATE templates/log/token_created.html
    This partial is swapped into #token-result after POST /log/tokens/create.

    Structure:
    - If error is set: render an alert-error div with the error message
    - If full_token is set: render the show-once reveal banner:
      - div with alert-warning styling + strong "Copy your token now — it will not be shown again" heading
      - code block (font-mono text-sm break-all) displaying {{ full_token }}
      - "Copy to clipboard" button using onclick="navigator.clipboard.writeText('{{ full_token }}')"
      - Dismiss button: hx-get="/log/tokens" hx-target="#token-list" hx-swap="innerHTML"
        label: "I've copied it — close"

    Example structure (adapt to project CSS classes):
    {% if error %}
    <div class="alert-error">
      <p>{{ error }}</p>
    </div>
    {% elif full_token %}
    <div class="alert-warning space-y-3">
      <p class="font-semibold">Copy your token now — it will not be shown again:</p>
      <code class="block font-mono text-sm break-all bg-yellow-50 dark:bg-yellow-900/20 p-2 rounded">{{ full_token }}</code>
      <div class="flex gap-3">
        <button type="button" class="btn-sm"
                onclick="navigator.clipboard.writeText('{{ full_token }}')">
          Copy to clipboard
        </button>
        <button type="button" class="btn-sm"
                hx-get="/log/tokens"
                hx-target="#token-list"
                hx-swap="innerHTML">
          I've copied it — close
        </button>
      </div>
    </div>
    {% endif %}

    ---

    CREATE templates/log/tokens_list.html
    This partial is loaded into #token-list via hx-trigger="load" and after revoke.

    Structure:
    - If tokens list is empty: render empty-state card matching log_table.html pattern
      (card > card-body > centered message "No API tokens yet")
    - If tokens exist: render table-wrap > data-table with columns:
      Token Prefix | Label | Created | Expires | Actions

    Each row has id="token-row-{{ token.id }}" for HTMX outerHTML swap on revoke.

    Token prefix cell: <code class="font-mono text-xs">{{ token.token_prefix }}...</code>
    Expires cell: {{ token.expires_at.strftime('%Y-%m-%d') if token.expires_at else 'Never' }}
    Created cell: {{ token.created_at.strftime('%Y-%m-%d') }}

    Revoke button per row (follow qso_row.html hx-delete pattern exactly):
      <button class="btn-danger btn-sm"
              hx-delete="/log/tokens/{{ token.id }}"
              hx-target="#token-row-{{ token.id }}"
              hx-swap="outerHTML"
              hx-confirm="Revoke '{{ token.name }}'? This cannot be undone.">
        Revoke
      </button>

    ---

    MODIFY templates/log/profile.html
    Add two new sections below the closing </div> of the existing profile form card
    (before the </div> that closes the outer max-w-3xl container):

    Section 1 — API Tokens header + creation form (card):
      - card-header: "API Tokens" title + hint text
      - card-body: form with hx-post="/log/tokens/create" hx-target="#token-result" hx-swap="innerHTML"
        - Token name input (required): name="name", type="text", class="form-input",
          placeholder="e.g. my-logger", label "Token Name"
        - Expiry date input (optional): name="expires_at", type="date", class="form-input",
          label "Expires" + hint "Leave blank for a token that never expires"
        - Submit button: class="btn-primary" text "Create Token"
      - Result target div: <div id="token-result"></div> (below the form, inside card-body)

    Section 2 — Active tokens list (loaded lazily):
      - card-header: "Active Tokens"
      - card-body:
        <div id="token-list"
             hx-get="/log/tokens"
             hx-trigger="load"
             hx-swap="innerHTML">
          <p class="text-sm text-gray-500">Loading tokens...</p>
        </div>

    ---

    CREATE tests/test_token_api.py — integration tests for the REST token CRUD API.

    Follow the existing test patterns in the project (use AsyncClient, pytest-asyncio,
    existing fixtures for authenticated clients).

    Test cases required:

    1. test_create_token_returns_full_token:
       POST /api/tokens with valid name, expect 201, response body contains full_token
       starting with "ollog_", and token_prefix is 8 chars.

    2. test_create_token_with_expiry:
       POST /api/tokens with name and expires_at="2099-12-31", expect 201,
       response expires_at is not None.

    3. test_create_token_invalid_name:
       POST /api/tokens with name containing spaces or special chars, expect 422.

    4. test_list_tokens_empty:
       GET /api/tokens on a fresh user, expect 200 and empty list [].

    5. test_list_tokens_after_create:
       Create a token, then GET /api/tokens — expect list with 1 entry, no full_token
       field in list response items.

    6. test_revoke_token:
       Create a token, DELETE /api/tokens/{id}, expect 204.
       Then GET /api/tokens — token no longer appears in list.

    7. test_revoke_other_users_token_returns_404:
       Create a token as user A, attempt DELETE as user B — expect 404.
       (Use two separate authenticated clients if fixtures support it, otherwise
       skip with pytest.mark.skip and a note if the test infrastructure doesn't
       support multi-user scenarios easily.)

    8. test_create_token_requires_auth:
       POST /api/tokens without Authorization header → expect 401 or 403.

    9. test_list_tokens_requires_auth:
       GET /api/tokens without Authorization header → expect 401 or 403.

    Check how existing test files in tests/ handle auth setup before writing —
    mirror the exact fixture pattern used (look at test_qso.py or test_auth.py
    for the authenticated client fixture).
  </action>
  <verify>
    cd /Users/royco/ollog && python -c "
    import jinja2
    env = jinja2.Environment(loader=jinja2.FileSystemLoader('templates'))
    env.get_template('log/token_created.html')
    env.get_template('log/tokens_list.html')
    env.get_template('log/profile.html')
    print('All templates parse OK')
    "
    cd /Users/royco/ollog && python -m pytest tests/test_token_api.py -v 2>&1 | tail -20
  </verify>
  <done>
    templates/log/token_created.html renders show-once banner when full_token is set,
    error message when error is set.
    templates/log/tokens_list.html renders token table with revoke buttons, or empty-state
    card when tokens list is empty.
    templates/log/profile.html has token creation form and lazy-loaded token list section.
    tests/test_token_api.py passes all 8 test cases (or documents skip reason for test 7).
    All Jinja2 templates parse without syntax errors.
  </done>
</task>

</tasks>

<verification>
Run the full verification sequence after all tasks complete:

1. Import health — all new modules load without errors:
   cd /Users/royco/ollog && python -c "from app.main import app; print('app OK')"

2. Route registration — token routes exist:
   cd /Users/royco/ollog && python -c "
   from app.main import app
   paths = [r.path for r in app.routes]
   assert '/api/tokens' in paths or any('/api/tokens' in p for p in paths)
   assert any('/log/tokens' in p for p in paths)
   print('Routes registered:', [p for p in paths if 'token' in p.lower()])
   "

3. Model field present:
   cd /Users/royco/ollog && python -c "
   from app.tokens.models import ApiToken
   assert hasattr(ApiToken.model_fields, 'expires_at')
   print('expires_at field present')
   "

4. Service helper works:
   cd /Users/royco/ollog && python -c "
   from app.tokens.service import token_is_active
   from unittest.mock import MagicMock
   from datetime import datetime, timezone, timedelta
   t = MagicMock(); t.enabled = True; t.expires_at = None
   assert token_is_active(t) == True
   t.enabled = False
   assert token_is_active(t) == False
   t.enabled = True; t.expires_at = datetime.now(tz=timezone.utc) - timedelta(days=1)
   assert token_is_active(t) == False
   print('token_is_active logic correct')
   "

5. Integration tests pass:
   cd /Users/royco/ollog && python -m pytest tests/test_token_api.py -v

6. Full test suite still passes (no regressions):
   cd /Users/royco/ollog && python -m pytest --tb=short -q
</verification>

<success_criteria>
Phase 26 is complete when ALL of the following are true:

- POST /api/tokens (Bearer JWT) creates a token and returns full_token exactly once (201)
- GET /api/tokens (Bearer JWT) returns active tokens with prefix, name, dates — no full_token
- DELETE /api/tokens/{id} (Bearer JWT) sets enabled=False and returns 204
- POST /log/tokens/create (cookie auth, HTMX) returns token_created.html partial with full_token on success, error partial on validation failure — always HTTP 200
- GET /log/tokens (cookie auth, HTMX) returns tokens_list.html partial — always HTTP 200
- DELETE /log/tokens/{id} (cookie auth, HTMX) sets enabled=False, returns empty 200 (row removed by outerHTML swap)
- templates/log/profile.html includes token creation form and lazy-loaded token list
- token_is_active() in service.py returns False for disabled tokens and False for expired tokens
- ApiToken model has expires_at: Optional[datetime] = None
- All 8 integration test cases in tests/test_token_api.py pass (or test 7 is skipped with documented reason)
- Full test suite has no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/026-token-crud-api-and-profile-ui/026-01-SUMMARY.md`
following the template at /Users/royco/.claude/get-shit-done/templates/summary.md
</output>
