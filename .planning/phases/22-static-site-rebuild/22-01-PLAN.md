---
phase: 22-static-site-rebuild
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site/
autonomous: true
must_haves:
  truths:
    - "mkdocs build completes without errors"
    - "site/ directory contains pages reflecting UDP documentation from Phases 19-21"
    - "Updated site/ is committed to the repository"
  artifacts:
    - path: "site/index.html"
      provides: "Root of rebuilt static site"
    - path: "site/deployment/index.html"
      provides: "Built deployment guide including UDP env vars and Docker Compose example"
    - path: "site/getting-started/index.html"
      provides: "Built getting-started guide including Sending QSOs via UDP section"
    - path: "site/troubleshooting/index.html"
      provides: "Built troubleshooting guide including four UDP entries"
  key_links:
    - from: "docs/deployment.md"
      to: "site/deployment/index.html"
      via: "mkdocs build"
      pattern: "UDP_ENABLED|UDP_PORT|UDP_BIND_HOST|UDP_OPERATOR"
    - from: "docs/getting-started.md"
      to: "site/getting-started/index.html"
      via: "mkdocs build"
      pattern: "Sending QSOs via UDP"
    - from: "docs/troubleshooting.md"
      to: "site/troubleshooting/index.html"
      via: "mkdocs build"
      pattern: "UDP"
---

<objective>
Rebuild the MkDocs static site so site/ reflects all UDP documentation added in Phases 19–21 and commit the result.

Purpose: The FastAPI app serves site/ at /guide via StaticFiles. Without a rebuild, operators visiting /guide still see the pre-v1.5 docs that omit UDP configuration, getting-started steps, and troubleshooting entries. This plan closes that gap.
Output: Updated site/ tree committed to git, completing v1.5.
</objective>

<execution_context>
@/Users/royco/.claude/get-shit-done/workflows/execute-plan.md
@/Users/royco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/royco/ollog/.planning/PROJECT.md
@/Users/royco/ollog/.planning/ROADMAP.md
@/Users/royco/ollog/mkdocs.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the static site with mkdocs</name>
  <files>site/</files>
  <action>
From the repo root /Users/royco/ollog, ensure mkdocs-material is available and run the build.

Step 1 — Check availability:
```
cd /Users/royco/ollog && python -m mkdocs --version
```
If the command fails (ModuleNotFoundError or command not found), install via:
```
pip install "mkdocs-material==9.*"
```

Step 2 — Build:
```
cd /Users/royco/ollog && python -m mkdocs build
```
The build writes output to site/ (configured in mkdocs.yml, already exists in repo from v1.3).

Do NOT pass --clean unless the build fails with a stale file error. The existing site/ tree is committed and --clean would wipe untracked assets.
  </action>
  <verify>
1. The build command exits with code 0 and no ERROR lines in its output.
2. Check three key output files contain UDP content:
   - `grep -i "UDP_ENABLED" /Users/royco/ollog/site/deployment/index.html` returns a match
   - `grep -i "Sending QSOs via UDP" /Users/royco/ollog/site/getting-started/index.html` returns a match
   - `grep -i "UDP" /Users/royco/ollog/site/troubleshooting/index.html` returns a match
  </verify>
  <done>mkdocs build exits 0 and all three grep checks match, confirming UDP docs from Phases 19–21 are present in the built output.</done>
</task>

<task type="auto">
  <name>Task 2: Commit updated site/ to git</name>
  <files>site/</files>
  <action>
Stage and commit the rebuilt site/ directory:

```
cd /Users/royco/ollog
git add site/
git commit -m "docs(22-01): rebuild static site with UDP documentation (v1.5)"
```

The commit message follows the pattern established by earlier doc builds (e.g., v1.3 static site commit). It should reference the phase and the milestone so the rebuild is traceable in git log.

Do NOT amend any prior commit. This is a new commit on top of the Phase 19–21 doc source commits.
  </action>
  <verify>
```
git -C /Users/royco/ollog log --oneline -1
```
Output should show the new commit message containing "rebuild static site".

```
git -C /Users/royco/ollog diff HEAD~1 --stat | grep "site/"
```
Output should list site/ files as changed, confirming the commit contains the rebuilt output.
  </verify>
  <done>A new git commit exists whose diff includes site/ files and whose message references the static site rebuild.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `python -m mkdocs build` in /Users/royco/ollog exits 0 with no errors.
2. `grep -i "UDP_ENABLED" site/deployment/index.html` matches.
3. `grep -i "Sending QSOs via UDP" site/getting-started/index.html` matches.
4. `grep -i "UDP" site/troubleshooting/index.html` matches.
5. `git log --oneline -1` shows the rebuild commit.
</verification>

<success_criteria>
- mkdocs build completes without errors (exit code 0).
- site/ output contains all UDP documentation from Phases 19–21.
- Updated site/ is committed to the repository, completing the v1.5 milestone.
</success_criteria>

<output>
After completion, create `/Users/royco/ollog/.planning/phases/22-static-site-rebuild/22-01-SUMMARY.md`
</output>
