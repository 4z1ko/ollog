---
phase: 05-multi-operator-live-feed
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/conftest.py
  - tests/test_operator_isolation.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Concurrent write integration tests (test_concurrent_writes.py) pass or skip gracefully when MongoDB is running as a replica set"
    - "Operator isolation integration tests (test_operator_isolation.py) pass or skip gracefully when MongoDB is running as a replica set"
  artifacts:
    - path: "tests/conftest.py"
      provides: "test_db fixture with directConnection=true URI"
      contains: "directConnection=true"
    - path: "tests/test_operator_isolation.py"
      provides: "isolation_test_db fixture with directConnection=true URI"
      contains: "directConnection=true"
  key_links:
    - from: "tests/conftest.py"
      to: "MongoDB (Docker replica set or standalone)"
      via: "AsyncMongoClient with directConnection=true"
      pattern: "directConnection=true"
    - from: "tests/test_operator_isolation.py"
      to: "MongoDB (Docker replica set or standalone)"
      via: "AsyncMongoClient with directConnection=true"
      pattern: "directConnection=true"
---

<objective>
Fix MongoDB connection URIs in two test fixtures so integration tests work with the replica set MongoDB configured in 05-01.

Purpose: The concurrent write tests (MULTI-01) and operator isolation tests (MULTI-02) all ERROR because their fixtures connect with a standalone URI that is incompatible with the replica set. Adding `directConnection=true` to the URI lets the driver connect directly to the node, working whether MongoDB is standalone or a replica set member.

Output: Two modified test files with working fixtures; all 8 integration tests pass (or skip if MongoDB is not running).
</objective>

<execution_context>
@/Users/royco/.claude/get-shit-done/workflows/execute-plan.md
@/Users/royco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-multi-operator-live-feed/05-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix test_db fixture URI in conftest.py</name>
  <files>tests/conftest.py</files>
  <action>
In `tests/conftest.py`, change the MongoDB URI on line 23 from:
```
"mongodb://localhost:27017",
```
to:
```
"mongodb://localhost:27017/?directConnection=true",
```

Do NOT change any other code in the file. Do not modify the socket check skip guard, the serverSelectionTimeoutMS, the database name, or any test logic. This is a single string change.
  </action>
  <verify>Run `cd /Users/royco/ollog && python -m pytest tests/test_concurrent_writes.py -v 2>&1 | tail -20` — tests should PASS (if Docker MongoDB running) or SKIP (if not running), but must NOT show ServerSelectionTimeoutError.</verify>
  <done>The test_db fixture in conftest.py uses directConnection=true and the 4 concurrent write tests no longer ERROR.</done>
</task>

<task type="auto">
  <name>Task 2: Fix isolation_test_db fixture URI in test_operator_isolation.py</name>
  <files>tests/test_operator_isolation.py</files>
  <action>
In `tests/test_operator_isolation.py`, change the MongoDB URI on line 107 from:
```
"mongodb://localhost:27017",
```
to:
```
"mongodb://localhost:27017/?directConnection=true",
```

Do NOT change any other code in the file. Do not modify the _mongo_available() helper, the serverSelectionTimeoutMS, the database name, the document_models list, or any test logic. This is a single string change.
  </action>
  <verify>Run `cd /Users/royco/ollog && python -m pytest tests/test_operator_isolation.py -v 2>&1 | tail -20` — tests should PASS (if Docker MongoDB running) or SKIP (if not running), but must NOT show ServerSelectionTimeoutError.</verify>
  <done>The isolation_test_db fixture in test_operator_isolation.py uses directConnection=true and the 4 operator isolation integration tests no longer ERROR.</done>
</task>

</tasks>

<verification>
Run the full test suite to confirm no regressions:
```bash
cd /Users/royco/ollog && python -m pytest tests/ -v 2>&1 | tail -40
```
- All tests should PASS or SKIP (no ERRORs)
- No other test files are affected (they were not modified)
</verification>

<success_criteria>
- tests/conftest.py contains `directConnection=true` in the MongoDB URI
- tests/test_operator_isolation.py contains `directConnection=true` in the MongoDB URI
- `pytest tests/test_concurrent_writes.py` shows 4 PASSED or 4 SKIPPED (zero ERRORS)
- `pytest tests/test_operator_isolation.py` shows 4-5 PASSED or SKIPPED (zero ERRORS)
- No other test files were modified
</success_criteria>

<output>
After completion, create `.planning/phases/05-multi-operator-live-feed/05-04-SUMMARY.md`
</output>
