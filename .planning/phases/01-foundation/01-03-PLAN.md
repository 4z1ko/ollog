---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - app/qso/__init__.py
  - app/qso/models.py
  - app/database.py
  - app/utils.py
  - tests/test_qso_schema.py
autonomous: true

must_haves:
  truths:
    - "QSO document model exists with compound unique index on {_operator, CALL, qso_date_utc, BAND, MODE}"
    - "Arbitrary ADIF fields are stored via extra='allow' without pre-declaring all 100+ fields"
    - "Soft-deleted QSOs have _deleted flag and default queries exclude them"
    - "UTC tzinfo is re-attached after every MongoDB read via from_mongo_dt utility"
  artifacts:
    - path: "app/qso/models.py"
      provides: "QSO Beanie Document with compound unique index"
      contains: "operator_qso_unique"
      min_lines: 30
    - path: "app/utils.py"
      provides: "from_mongo_dt() UTC re-attachment utility"
      exports: ["from_mongo_dt"]
    - path: "app/database.py"
      provides: "Updated init_beanie with QSO in document_models"
      contains: "QSO"
    - path: "tests/test_qso_schema.py"
      provides: "Schema and index verification tests"
      contains: "test_"
  key_links:
    - from: "app/qso/models.py"
      to: "pymongo.IndexModel"
      via: "compound unique index definition"
      pattern: "IndexModel.*operator_qso_unique"
    - from: "app/database.py"
      to: "app/qso/models.py"
      via: "QSO in document_models list"
      pattern: "document_models.*QSO"
    - from: "app/qso/models.py"
      to: "ConfigDict"
      via: "extra=allow for arbitrary ADIF fields"
      pattern: 'extra.*allow'
---

<objective>
Define the QSO Beanie Document model with ADIF field names as document keys, compound unique index, soft-delete flag, and UTC datetime utility. Register the model in init_beanie.

Purpose: The QSO schema and indexes must be correct before any writes happen. The compound unique index prevents duplicate QSOs under concurrent access. The UTC utility prevents datetime bugs across the entire codebase.
Output: QSO model with indexes, from_mongo_dt utility, updated database.py, schema tests.
</objective>

<execution_context>
@/Users/royco/.claude/get-shit-done/workflows/execute-plan.md
@/Users/royco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: QSO model, UTC utility, and database registration</name>
  <files>app/qso/models.py, app/qso/__init__.py, app/utils.py, app/database.py</files>
  <action>
Create app/utils.py with from_mongo_dt() function:
```python
from datetime import datetime, timezone

def from_mongo_dt(dt: datetime | None) -> datetime | None:
    """Re-attach UTC tzinfo to a datetime read from MongoDB.
    PyMongo may return naive datetimes — always call this after reads."""
    if dt is None:
        return None
    if dt.tzinfo is None:
        return dt.replace(tzinfo=timezone.utc)
    return dt
```

Create app/qso/models.py with QSO Beanie Document:
- Import: pymongo, IndexModel from pymongo, Document from beanie, ConfigDict from pydantic, Optional from typing, datetime
- class QSO(Document):
  - model_config = ConfigDict(extra="allow")  # stores arbitrary ADIF fields verbatim
  - Declared fields (for indexing and type safety):
    - operator: str = Field(alias="_operator")  # NOTE: Beanie uses underscore-prefixed fields specially. Use Field with alias, or use a non-underscore field name internally and alias to _operator in MongoDB. Test which approach works with Beanie 2.x. If alias doesn't work with Beanie, use "operator_callsign" as the Python field name with a MongoDB alias. The key requirement is that the MongoDB document has `_operator` as the field name for indexing.
    - CALL: str
    - BAND: str | None = None
    - MODE: str | None = None
    - qso_date_utc: datetime | None = None
    - deleted: bool = Field(default=False, alias="_deleted")  # same alias consideration as above
  - class Settings:
    - name = "qsos"
    - indexes = [
        IndexModel(
            [("_operator", pymongo.ASCENDING), ("CALL", pymongo.ASCENDING), ("qso_date_utc", pymongo.ASCENDING), ("BAND", pymongo.ASCENDING), ("MODE", pymongo.ASCENDING)],
            unique=True,
            name="operator_qso_unique",
        ),
        IndexModel([("_operator", pymongo.ASCENDING)], name="operator_idx"),
        IndexModel([("_operator", pymongo.ASCENDING), ("_deleted", pymongo.ASCENDING)], name="operator_active_idx"),
      ]

IMPORTANT: Beanie Document has its own `id` field that maps to MongoDB `_id`. Fields starting with underscore in Pydantic/Beanie are treated as private. Use Pydantic's Field(alias="...") to map Python-friendly names to underscore MongoDB field names. Verify this works — if Beanie 2.x doesn't support alias on Document fields properly, store as "operator_callsign" in Python and use a pre-save hook or just store as "operator" in MongoDB (without underscore) and update the index definition. The CONTEXT.md decision says "_operator" is the MongoDB field name — honor this.

Update app/database.py:
- Import QSO from app.qso.models
- Add QSO to the document_models list in init_beanie call
- Keep the list extensible (01-04 will add User model)

Update app/qso/__init__.py to export QSO.
  </action>
  <verify>python -c "from app.qso.models import QSO; print(QSO.Settings.name, len(QSO.Settings.indexes))" should print "qsos 3"</verify>
  <done>QSO model defined with compound unique index, extra="allow" for arbitrary ADIF fields, soft-delete flag, and _operator field. from_mongo_dt utility exists. database.py registers QSO in init_beanie.</done>
</task>

<task type="auto">
  <name>Task 2: Schema and index integration tests</name>
  <files>tests/test_qso_schema.py, tests/conftest.py</files>
  <action>
Update tests/conftest.py (if needed) to add a fixture for a real MongoDB connection for integration tests. Use Docker Compose MongoDB:
- pytest fixture (session-scoped) that connects to MongoDB at localhost:27017 (assumes docker compose up -d mongodb), creates a test database "ollog_test", runs init_beanie with [QSO], yields, then drops the test database and closes client.
- Mark integration tests with @pytest.mark.asyncio

Create tests/test_qso_schema.py with tests:

test_qso_collection_name: assert QSO.Settings.name == "qsos"

test_qso_compound_unique_index_exists: after init_beanie, list indexes on the qsos collection and verify "operator_qso_unique" index exists with the correct key pattern.

test_qso_extra_fields_stored: create a QSO with standard fields plus extra ADIF fields (e.g., RST_SENT="599", COMMENT="test"), insert it, read it back, verify the extra fields are present in the document.

test_qso_duplicate_rejected: insert two QSOs with identical (_operator, CALL, qso_date_utc, BAND, MODE) — second insert should raise DuplicateKeyError from pymongo.

test_qso_soft_delete_flag: create QSO with _deleted=False, update to _deleted=True, verify it can be queried with the flag.

test_from_mongo_dt_naive: pass a naive datetime to from_mongo_dt, verify it returns UTC-aware datetime.

test_from_mongo_dt_aware: pass an already-aware datetime, verify it's returned unchanged.

test_from_mongo_dt_none: pass None, verify None returned.

These tests require a running MongoDB instance. Add a pytest marker or skip condition if MongoDB is not available.
  </action>
  <verify>docker compose up -d mongodb && pytest tests/test_qso_schema.py -v && docker compose down</verify>
  <done>Schema tests pass: collection name correct, compound unique index created by init_beanie, extra ADIF fields stored and retrieved, duplicate insertion rejected, from_mongo_dt correctly handles all cases.</done>
</task>

</tasks>

<verification>
- QSO model has compound unique index on {_operator, CALL, qso_date_utc, BAND, MODE}
- extra="allow" on QSO model permits arbitrary ADIF fields
- from_mongo_dt() re-attaches UTC tzinfo to naive datetimes
- Duplicate QSO insertion raises error (index enforced)
- database.py includes QSO in init_beanie document_models
- No imports from motor anywhere
</verification>

<success_criteria>
QSO Beanie Document model exists with correct compound unique index. Arbitrary ADIF fields pass through via extra="allow". Soft-delete pattern established. UTC datetime utility works. All integration tests pass against real MongoDB.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
