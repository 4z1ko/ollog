---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - Dockerfile
  - docker-compose.yml
  - app/__init__.py
  - app/main.py
  - app/config.py
  - app/database.py
  - .env.example
autonomous: true

must_haves:
  truths:
    - "Application starts via Docker Compose with a single command and connects to MongoDB successfully"
    - "Health endpoint returns 200 with status info confirming MongoDB connectivity"
  artifacts:
    - path: "pyproject.toml"
      provides: "Project metadata and dependencies"
      contains: "fastapi"
    - path: "Dockerfile"
      provides: "Container build for FastAPI app"
      contains: "uvicorn"
    - path: "docker-compose.yml"
      provides: "Multi-service orchestration (api + mongodb)"
      contains: "service_healthy"
    - path: "app/main.py"
      provides: "FastAPI app with lifespan and health endpoint"
      contains: "lifespan"
    - path: "app/config.py"
      provides: "Typed settings from environment"
      contains: "BaseSettings"
    - path: "app/database.py"
      provides: "AsyncMongoClient + init_beanie initialization"
      contains: "AsyncMongoClient"
  key_links:
    - from: "app/main.py"
      to: "app/database.py"
      via: "lifespan calls init_db"
      pattern: "init_db|init_beanie"
    - from: "app/config.py"
      to: "docker-compose.yml"
      via: "environment variables match"
      pattern: "MONGODB_URI|MONGODB_DB"
    - from: "docker-compose.yml"
      to: "mongodb"
      via: "healthcheck + depends_on condition"
      pattern: "service_healthy"
---

<objective>
Create the project skeleton: pyproject.toml with all Phase 1 dependencies, FastAPI app with lifespan-based database initialization, pydantic-settings config, Docker Compose with MongoDB healthcheck, and a health endpoint.

Purpose: Every subsequent plan builds on this skeleton. The app must start and connect to MongoDB before any feature code is added.
Output: Running FastAPI app in Docker Compose with MongoDB connection confirmed via health endpoint.
</objective>

<execution_context>
@/Users/royco/.claude/get-shit-done/workflows/execute-plan.md
@/Users/royco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project files and FastAPI app with config</name>
  <files>pyproject.toml, app/__init__.py, app/main.py, app/config.py, app/database.py, app/auth/__init__.py, app/adif/__init__.py, app/qso/__init__.py, .env.example</files>
  <action>
Create pyproject.toml with:
- requires-python = ">=3.12"
- dependencies: fastapi[standard]>=0.135.0, beanie>=2.1.0, pymongo>=4.16.0, pyjwt>=2.12.0, pwdlib[argon2]>=0.3.0, pydantic-settings>=2.0
- dev dependency group: pytest>=8.0, pytest-asyncio>=0.23, httpx>=0.27
- Project name "ollog", version "0.1.0"

Create app/config.py with pydantic-settings BaseSettings class:
- mongodb_uri: str (default "mongodb://mongodb:27017")
- mongodb_db: str (default "ollog")
- secret_key: str (no default — must be set)
- jwt_algorithm: str = "HS256"
- jwt_expire_minutes: int = 60
- admin_username: str | None = None (for bootstrap)
- admin_password: str | None = None (for bootstrap)
- admin_callsign: str | None = None (for bootstrap)
- Use model_config with env_file=".env", env_file_encoding="utf-8"
- Instantiate a module-level `settings = Settings()` singleton

Create app/database.py:
- Import `AsyncMongoClient` from `pymongo` (NOT motor)
- Function `init_db()` that creates AsyncMongoClient from settings.mongodb_uri, calls `init_beanie` with database=client[settings.mongodb_db] and document_models=[] (empty list for now — 01-03 and 01-04 will add models)
- Function `close_db()` that closes the client
- Store client reference at module level for access by health check

Create app/main.py:
- Import asynccontextmanager from contextlib
- Create `lifespan` async context manager that calls init_db() on startup and close_db() on shutdown
- Create FastAPI app with lifespan=lifespan, title="ollog", version="0.1.0"
- Add GET /health endpoint that pings MongoDB (client.admin.command("ping")) and returns {"status": "ok", "mongodb": "connected"} on success or {"status": "error", "mongodb": "disconnected"} with 503 on failure

Create empty __init__.py files for app/, app/auth/, app/adif/, app/qso/ packages.

Create .env.example with all config variables documented with comments.
  </action>
  <verify>python -c "from app.config import settings" succeeds (with SECRET_KEY env var set). python -c "from app.main import app; print(app.title)" prints "ollog".</verify>
  <done>FastAPI app module exists with lifespan, config loads from environment, database.py has init/close functions using pymongo AsyncMongoClient (not motor).</done>
</task>

<task type="auto">
  <name>Task 2: Docker Compose and Dockerfile</name>
  <files>Dockerfile, docker-compose.yml</files>
  <action>
Create Dockerfile:
- FROM python:3.12-slim
- WORKDIR /app
- COPY pyproject.toml .
- RUN pip install --no-cache-dir . (installs from pyproject.toml)
- COPY app/ app/
- EXPOSE 8000
- CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]

Create docker-compose.yml:
- Service "mongodb": image mongo:7, ports 27017:27017, volume for data persistence (mongo-data:/data/db), healthcheck using mongosh --eval "db.adminCommand('ping')" with interval 10s, timeout 5s, retries 5, start_period 30s
- Service "api": build from ., ports 8000:8000, depends_on mongodb with condition: service_healthy, environment variables from .env file (env_file: .env), set SECRET_KEY to a dev default in docker-compose for local dev (e.g. "dev-secret-change-in-production")
- Named volume mongo-data at bottom

Verify the health endpoint works end-to-end:
- Run `docker compose up -d --build`
- Wait for healthy status
- curl http://localhost:8000/health should return {"status":"ok","mongodb":"connected"}
- Run `docker compose down`
  </action>
  <verify>docker compose up -d --build && sleep 10 && curl -s http://localhost:8000/health | python3 -c "import sys,json; d=json.load(sys.stdin); assert d['mongodb']=='connected'" && docker compose down</verify>
  <done>Docker Compose starts both services, MongoDB healthcheck passes, API connects to MongoDB, /health returns connected status.</done>
</task>

</tasks>

<verification>
- `docker compose up -d --build` starts without errors
- `curl http://localhost:8000/health` returns 200 with mongodb: connected
- `docker compose down` cleans up
- No imports from motor, python-jose, or passlib anywhere in codebase
- Config loads SECRET_KEY from environment
</verification>

<success_criteria>
Application starts via Docker Compose with a single command and connects to MongoDB. Health endpoint confirms connectivity. All Phase 1 dependencies are declared in pyproject.toml. Project structure matches the flat layout from research.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
