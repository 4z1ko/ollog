---
phase: 04-adif-import-export
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - app/adif/router.py
  - templates/log/import_report.html
autonomous: true

must_haves:
  truths:
    - "Import detects duplicates using find_duplicate() with the same +/-2 min fuzzy window as live entry"
    - "Duplicate records are NOT inserted — they appear in the duplicates list for operator review"
    - "Duplicate report entries include record_index, call, and existing_id of the matching QSO"
    - "Non-duplicate records are still inserted normally"
    - "Re-importing the same file produces all duplicates and zero accepted (idempotency proof)"
  artifacts:
    - path: "app/adif/router.py"
      provides: "Import endpoint with find_duplicate wired into loop"
      contains: "find_duplicate"
  key_links:
    - from: "app/adif/router.py"
      to: "app/qso/service.find_duplicate"
      via: "import and call per record"
      pattern: "find_duplicate\\("
---

<objective>
Wire find_duplicate() into the ADIF import loop so duplicate records are detected and reported instead of inserted. Uses the same +/-2 min fuzzy window as live QSO entry (ADIF-02).

Purpose: Operators see which imported records are potential duplicates of existing QSOs, enabling informed review without silent data loss or auto-deletion.
Output: Import endpoint now categorizes records into accepted/duplicates/errors with duplicate detection active.
</objective>

<execution_context>
@/Users/royco/.claude/get-shit-done/workflows/execute-plan.md
@/Users/royco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-adif-import-export/04-01-SUMMARY.md
@app/adif/router.py
@app/qso/service.py
@templates/log/import_report.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire find_duplicate into import loop and add integration tests</name>
  <files>app/adif/router.py, templates/log/import_report.html</files>
  <action>
In the `process_import()` helper function in `app/adif/router.py` (created in 04-01), add duplicate detection to the per-record loop:

After `build_qso_dict()` succeeds and before `QSO(**qso_dict).insert()`, add:

```python
dup = await find_duplicate(
    operator=operator,
    call=qso_dict["CALL"],
    band=qso_dict["BAND"],
    mode=qso_dict["MODE"],
    qso_date_utc=qso_dict["qso_date_utc"],
)
if dup is not None:
    duplicates.append({
        "record_index": idx,
        "call": qso_dict["CALL"],
        "existing_id": str(dup.id),
    })
    continue
```

Import `find_duplicate` from `app.qso.service` if not already imported.

The key insertion point is: after the try/except for build_qso_dict, before the QSO insert. The `continue` ensures duplicates skip insertion entirely.

Update `templates/log/import_report.html` if needed to properly display the duplicate entries — each duplicate row should show Record #, Callsign, and the existing QSO ID (or a link to it if practical).

Also create `tests/test_adif_import.py` with integration tests (require MongoDB via @mongo_required pattern from existing tests):
1. Test basic import: upload a valid 3-record ADIF string, verify 3 accepted, 0 duplicates, 0 errors
2. Test duplicate detection: import same file twice, verify second import has 0 accepted and 3 duplicates
3. Test missing required field: upload ADIF with one record missing CALL, verify 1 error and remaining records accepted
4. Test file size guard: send > 10 MB payload, verify 413 response
5. Test parse error handling: upload ADIF with a malformed tag, verify error appears in report

Follow existing test patterns: use `httpx.AsyncClient` with `app` from `app.main`, use `@pytest.mark.asyncio`, create auth fixtures that bootstrap a user and get a JWT token. Use the `aclose()` pattern for AsyncMongoClient teardown.
  </action>
  <verify>
Run: `python -m pytest tests/test_adif_import.py -x -v` — all import tests pass
Run: `python -m pytest tests/ -x -q` — no regressions
  </verify>
  <done>find_duplicate() called for every valid record during import. Duplicates skip insertion and appear in the duplicates list with record_index, call, existing_id. Re-importing the same file produces all duplicates. Integration tests confirm behavior.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_adif_import.py -v` — all import tests pass including duplicate detection
2. `python -m pytest tests/ -x -q` — no regressions
3. Import a file twice via API: first import has N accepted, second import has N duplicates, 0 accepted
</verification>

<success_criteria>
- Duplicate records detected via find_duplicate() during import
- Duplicates NOT inserted — appear in report for operator review
- Report includes record_index, call, existing_id for each duplicate
- Re-import of same file produces zero accepted, all duplicates
- All existing and new tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-adif-import-export/04-02-SUMMARY.md`
</output>
