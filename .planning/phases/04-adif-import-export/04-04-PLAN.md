---
phase: 04-adif-import-export
plan: 04
type: tdd
wave: 4
depends_on: ["04-02", "04-03"]
files_modified:
  - tests/test_adif_roundtrip.py
  - tests/fixtures/roundtrip_sample.adi
autonomous: true

must_haves:
  truths:
    - "An ADIF file exported from ollog and re-imported produces zero data changes — all records flagged as duplicates"
    - "APP_ fields survive import-export-reimport cycle with identical values"
    - "USERDEF fields survive import-export-reimport cycle with identical values"
    - "Parser handles files with missing EOH tag"
    - "Parser handles case-insensitive field names (e.g., <call:5> treated same as <CALL:5>)"
    - "Parser handles varying whitespace around EOR tags"
  artifacts:
    - path: "tests/test_adif_roundtrip.py"
      provides: "Round-trip and edge-case integration tests"
      contains: "test_roundtrip"
    - path: "tests/fixtures/roundtrip_sample.adi"
      provides: "Test ADIF file with APP_, USERDEF, and edge-case formatting"
  key_links:
    - from: "tests/test_adif_roundtrip.py"
      to: "/api/adif/import"
      via: "httpx POST"
      pattern: "post.*adif/import"
    - from: "tests/test_adif_roundtrip.py"
      to: "/api/adif/export"
      via: "httpx GET"
      pattern: "get.*adif/export"
---

<objective>
Validate ADIF round-trip fidelity and parser edge cases through integration tests. Proves that import-export-reimport produces zero data changes (ADIF-05) and that APP_/USERDEF fields survive the cycle (ADIF-03). Also tests parser robustness for missing EOH, case-insensitive field names, and varying EOR whitespace (ADIF-06).

Purpose: Proves the lossless round-trip guarantee that is the core promise of Phase 4.
Output: Comprehensive test suite and test fixture file.
</objective>

<execution_context>
@/Users/royco/.claude/get-shit-done/workflows/execute-plan.md
@/Users/royco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-adif-import-export/04-01-SUMMARY.md
@.planning/phases/04-adif-import-export/04-02-SUMMARY.md
@.planning/phases/04-adif-import-export/04-03-SUMMARY.md
@app/adif/parser.py
@app/adif/serializer.py
@app/adif/router.py
@app/qso/service.py
@app/qso/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test fixture ADIF file with APP_, USERDEF, and edge cases</name>
  <files>tests/fixtures/roundtrip_sample.adi</files>
  <action>
Create `tests/fixtures/` directory if it does not exist.

Create `tests/fixtures/roundtrip_sample.adi` — a valid ADIF file containing:

1. A standard header with ADIF_VER and PROGRAMID, terminated by `<EOH>`
2. At least 5 QSO records with variety:
   - Record 1: Standard QSO (CALL, QSO_DATE, TIME_ON, BAND, MODE, FREQ, RST_SENT, RST_RCVD)
   - Record 2: QSO with APP_ fields (APP_MYLOGGER_SCORE, APP_CONTEST_ID) — tests ADIF-03
   - Record 3: QSO with USERDEF-style fields (MY_ANTENNA, MY_RIG or similar) — tests ADIF-03
   - Record 4: QSO with mixed case field names in the raw ADIF (e.g., `<call:5>` and `<Band:3>`) — tests ADIF-06 case-insensitive parsing
   - Record 5: QSO with extra whitespace/newlines between fields and after `<EOR>` — tests ADIF-06 whitespace handling

Also create a second fixture `tests/fixtures/no_eoh_sample.adi` — same as above but with NO `<EOH>` tag, no header section, just records. Tests ADIF-06 missing EOH handling.

Use realistic amateur radio data: callsigns like W1AW, JA1ABC, DL1XYZ; bands like 20M, 40M; modes like SSB, CW, FT8; dates in YYYYMMDD format; times in HHMM or HHMMSS format.
  </action>
  <verify>
Run: `python -c "from app.adif.parser import parse_adi; r,e = parse_adi(open('tests/fixtures/roundtrip_sample.adi').read()); print(f'{len(r)} records, {len(e)} errors')"` — should show 5 records, 0 errors.
Run: `python -c "from app.adif.parser import parse_adi; r,e = parse_adi(open('tests/fixtures/no_eoh_sample.adi').read()); print(f'{len(r)} records, {len(e)} errors')"` — should show records, 0 errors.
  </verify>
  <done>Test fixture files created with APP_ fields, USERDEF fields, mixed-case field names, extra whitespace, and a no-EOH variant. All parseable by parse_adi().</done>
</task>

<task type="auto">
  <name>Task 2: Create round-trip and edge-case integration tests</name>
  <files>tests/test_adif_roundtrip.py</files>
  <action>
Create `tests/test_adif_roundtrip.py` with the following integration tests. Use @mongo_required pattern, httpx.AsyncClient, aclose() teardown — same patterns as existing tests.

**Test 1: test_full_roundtrip_zero_changes**
- Import `roundtrip_sample.adi` via POST /api/adif/import
- Verify N records accepted, 0 duplicates, 0 errors
- Export via GET /api/adif/export
- Parse the exported text with parse_adi()
- Verify same number of records as imported
- Re-import the exported text via POST /api/adif/import
- Verify 0 accepted (all duplicates) — proves lossless round-trip (ADIF-05)

**Test 2: test_app_fields_preserved**
- Import the fixture file
- Export via GET /api/adif/export
- Parse exported ADIF
- Find the record with APP_MYLOGGER_SCORE (or APP_CONTEST_ID)
- Assert the APP_ field value matches the original fixture value exactly (ADIF-03)

**Test 3: test_userdef_fields_preserved**
- Same as Test 2 but for the USERDEF/custom fields (MY_ANTENNA etc.)
- Assert the custom field value matches the original fixture value (ADIF-03)

**Test 4: test_missing_eoh_file**
- Import `no_eoh_sample.adi` via POST /api/adif/import
- Verify records are accepted (parser handles missing EOH per ADIF-06)

**Test 5: test_case_insensitive_field_names**
- Import the fixture file (record 4 has mixed-case field names)
- Verify the record is accepted
- Export and verify field names are UPPERCASE in output (parser normalizes to uppercase per ADIF-06)

**Test 6: test_whitespace_around_eor**
- Import the fixture file (record 5 has extra whitespace)
- Verify the record is accepted without errors (ADIF-06)

**Test 7: test_export_does_not_contain_internal_fields**
- Import some QSOs, export
- Assert "qso_date_utc" does NOT appear anywhere in the exported ADIF text
- Assert "_operator" does NOT appear in the exported ADIF text
- Assert "_deleted" does NOT appear in the exported ADIF text

Each test should set up its own operator (or share a fixture) and clean up. Follow the existing pattern of database initialization in fixtures.
  </action>
  <verify>
Run: `python -m pytest tests/test_adif_roundtrip.py -x -v` — all 7 tests pass
Run: `python -m pytest tests/ -x -q` — no regressions
  </verify>
  <done>Round-trip tests prove: export-reimport produces zero data changes, APP_ fields preserved, USERDEF fields preserved, missing EOH handled, case-insensitive fields normalized, whitespace tolerated, internal fields excluded from export. All success criteria for ADIF-03, ADIF-05, ADIF-06 validated.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_adif_roundtrip.py -v` — all round-trip and edge-case tests pass
2. `python -m pytest tests/ -x -q` — full test suite passes, no regressions
3. Round-trip test proves: import N records, export, reimport = 0 accepted, N duplicates
</verification>

<success_criteria>
- Full round-trip (import -> export -> reimport) produces zero data changes
- APP_ fields survive the cycle with identical values
- USERDEF/custom fields survive the cycle with identical values
- Parser handles missing EOH, case-insensitive names, whitespace variants
- Internal fields (qso_date_utc, _operator, _deleted) never appear in exported ADIF
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-adif-import-export/04-04-SUMMARY.md`
</output>
